# Changelog

All notable changes to **GAS ShortCut OPS** (SilverFox Marketing's Vehicle Graphic Production System) are documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).
Entries are grouped under **Added**, **Changed**, **Fixed**, and **Removed**.
Commit messages follow [Conventional Commits](https://www.conventionalcommits.org/) (`feat:`, `fix:`, `docs:`, `refactor:`, `chore:`).

---

## [Unreleased]

### Changed
- Core sheet/folder ID constants (MASTER/CONFIG/TEMPLATE/OUTPUT_FOLDER/VIN_LOGS + LOT_SUBMISSIONS/EOM_INDEX) now resolve through a `ScriptApp.getScriptId()`-keyed `ENV_IDS` map in Code.gs Section 1 — the scriptId IS the environment. Prod entry only for now (dev script entry lands with the dev environment); an unregistered scriptId (e.g. a stray Drive copy of the master) throws at load instead of running against prod IDs. Constant names unchanged — zero call-site changes. lot-scan/eom-viewer keep hardcoded prod IDs until they get dev twins

### Added
- Home dashboard endpoints (Code.gs Section 34): `getHomeHud()` (today / Monday-start-week / all-time run stats from one RUN_LOG read, test runs excluded), `getDealerSummary(dealerKey)` (inventory from IMPORT_STATS last import via scraper-location join, per-dealer run stats + latest run, EOM order count), `getInventorySnapshot()` (server-side slice of the DASHBOARD sheet's inventory block). EOM counts come from a products-free Pipedrive stage-deals list cached 10 min (ScriptCache `eom_org_counts_v1`), org ids deduped across a dealer's groups, fail-soft null — a Pipedrive failure can never break Home
- VIN Inbox "Create order" batch action (replaces "Copy valid VINs") — jumps to Run Order with the batch's dealer preselected and its valid VINs appended (`runPrefillFromInbox`, mirroring the VIN Log jump precedent, + `CustomSelect.refresh` so the styled dropdown facade updates); once every result card is resolved with at least one finalized, prompts once to discard the source batch via the bulk endpoint. Cancelling the dealer-switch guard, switching dealers, or an all-abandoned run kills the pending link (fail-safe: batch rows stay in the inbox)
- "Icon Rail" nav layout in UI Settings (`ui_nav_layout='icons'` → `data-nav="icons"`) — the collapsed hover-expanding sidebar, formerly reachable only via the Slate theme or Auto-hide
- `--fs-lg` (16px) added to the base type scale in both SharedUtils copies — referenced by VIN Inbox / End of Month / lot-scan Capture but never defined (tokens debt)
- Contained dev environment: DEV_ copies of all six core spreadsheets + DEV_QR/DEV_OUTPUT/DEV_EOM_REPORTS Drive folders in the Claude Sandbox folder, a dev Apps Script project bound to DEV_SF_SYSTEM_MASTER (registered in `ENV_IDS`), and a neutralized dev config (all dealer QR/output folders + `eom_reports_folder_id` → dev folders; audited: the three Drive write sinks are all contained). `.clasp.json` now targets DEV — default `clasp push` is safe; `.clasp.prod.json` holds the prod target for the guarded promote script. Inventory + runbook: `docs/dev-environment.md`
- `sweepLotPhotos()` editor utility (Code.gs Section 32) trashes the Drive photos of already-discarded/processed backlog rows; logs and returns `sweepLotPhotos: scanned=N trashed=N failed=N`. Idempotent (`setTrashed` on an already-trashed file is a no-op); crew-owned files the office can't trash just log/count. Rows are still retained (T5.2)
- Office-side manual OCR runner (Code.gs Section 32: `extractVinFromImage_`, `extractVinCandidates_`, `analyzeDriveFile_`, `runInboxOcr`) + VIN Inbox "Run OCR (N queued)" button — replaces the Lot Scanner's per-user `drainOcrQueue` trigger, which stalled in the field (batches sent to the office before OCR drained sat at `ocr_state='queued'` forever). Processes submitted rows (drafts are OCR'd only after the crew sends the batch to the office). Main app gains the Drive v2 advanced service (T3.1)
- Lot Scanner Drafts — "Add more photos" resumes a draft batch into the capture flow (shared `vpBeginShooting`); appended photos commit under the same batchId and fold into the same batch card (T5.4)
- Lot Scanner Drafts: OCR progress chips + auto-refresh while OCR drains (visibility-gated), edit-safe re-renders
- `updateVinSubmissionStatuses` bulk server fn (Code.gs Section 32) — VIN Inbox's "Discard batch" now issues one bulk call instead of a serial per-row loop (T2.2)
- Unified component layer in SharedUtils (canonical buttons/pills/tags/table, --font-mono token) — SuperDesign variant 1 spec
- Visual nesting hierarchy for targeting-rule groups in Dealer Rules → Filtering Rules (rails, depth steps, AND/OR chips) — F3
- VIN Inbox: batches collapsible via native `<details>` (collapsed by default) with a submission count in the summary, plus a "Discard batch" action that serially discards every submission in a batch and refreshes the list — F4

### Changed
- VIN Logs opens on "-- All Dealers --" showing the full run log (newest first, Dealer column added in that mode) instead of a blank page behind "-- Select a dealer --". `getRunsForDealer('')` = no filter, and every run row now carries `dealerKey`/`dealerName` so commit / rollback / Pipedrive push resolve the run's own dealer in the mixed table (the dealer select is empty in that mode). Latest-order pill + manual VIN entry still require picking a specific dealer
- Home reworked from a raw DASHBOARD-grid dump into a HUD — status strip + workflow cards stay; below them: a compact All-Time strip (runs, VINs, avg/run, committed/pending/rolled back), Today and This Week tile rows (runs, VINs produced, dealers served, duplicates), and a Dealer Focus card (dealer dropdown → inventory totals with as-of, run stats + per-type, most recent run with status tag, EOM open-order count). All states handled (loading, no inventory for location, EOM unavailable, zero runs, stale-response guard on fast dealer switching). Layout pass per design review: two-column HUD (System Stats | Dealer Focus) via container-measured auto-fit grid, Today → This Week → All Time order with a hero/standard/ambient size gradient, accent section labels + accent edges on the Today group and focus card, and a visible bg → panel → tile surface step in both themes
- Inventory Snapshot moved to the Import Data screen — renders in the right column while no files are staged (yields to file cards, reappears and refetches after an import), `.table-u` with a bold TOTALS row. Distinct from Browse Current Data (per-location totals, not raw rows). Layout pass per design review: file cards moved to the left column (mode picker → cards → file input → status/action row, replacing the far-away full-width footer); the snapshot owns the full right column, always visible, flexing to panel height; view bumped from the form-wide (1280px) to the data (1680px) measure tier so the snapshot table uses the full window instead of forcing a horizontal scroll
- VIN Inbox restyled to the EOM report idiom — batch `<details>` cards with caret + prominent dealer title, right-aligned `.eomr-chip` stat pills (N submissions / N read / N valid), indented card body; submission cards tightened (smaller thumb, denser grid, single muted `email · timestamp` meta line). Deliberate `eomr-*` class reuse (EOM restyles propagate here by intent); `.vi-copy`/`.vi-refresh` dialect migrated to canonical `btn-secondary`/`btn-ghost`
- VIN Inbox timestamps now render "Thu Jul 09 2026 1:42 PM" — `getVinSubmissions` formats Date cells via `Utilities.formatDate` (was raw `Date.toString()` with GMT offset noise) and returns a numeric `tsMs` the client now sorts on (was `Date.parse` of the display string)
- Themes are palette-only — nav layout now lives solely in UI Settings. "Top Rail" / "Top Rail — Dark" themes removed (saved prefs remap to Light/Dark at read time in `getThemePreference`, no write-back); "Gruvbox Rail" → **Gruvbox** and "Slate — Icon Rail" → **Slate**, demoted to pure recolors (ids `gruvbox-rail`/`slate-icons` kept — they're persistence keys). The "Auto — follow the theme" nav option is gone; a stale saved `'auto'` falls back to Sidebar via the `UI_NAV_LAYOUTS_` allow-list check
- Lot Scanner photos now land on a Shared Drive — `LOT_PHOTOS_FOLDER_ID` swapped to the new "SF Lot Submissions" Shared-Drive folder. Files there are org-owned, so office discard/processed photo-trash works regardless of which crew member shot the photo (My-Drive files were owner-trashable only; all app users need Content Manager). Dealer subfolders auto-recreate; old-folder photos still read fine (fileId-based) and trashing them fails soft
- VIN Inbox discard AND processed now best-effort-trash the submission's Drive photo (single `updateVinSubmissionStatus` + bulk `updateVinSubmissionStatuses` paths, via `trashLotPhoto_`), with untrashable files (e.g. crew-owned My-Drive photos the office can't trash) counted and surfaced in the toast. Rows are still retained; PHOTO_ID is not blanked (keeps the sweep idempotent) (T5.2)
- VIN Inbox now shows SENT submissions only — `getVinSubmissions` default filter and `runInboxOcr` both exclude `status='draft'` (were draft+submitted) so the client Run-OCR count and the server OCR pass agree; drafts stay field-owned until "Send to office"
- Lot Scanner is capture/upload/batch/dealer/send only — OCR (`drainOcrQueue`/`ensureOcrTrigger` + the per-user time trigger) is stripped now that the office runs it manually from VIN Inbox (T3.2). Drafts loses its OCR-drain poll (interval + visibilitychange listener) and the "N/M read" chip; a draft's rows still land `ocr_state='queued'` when no barcode matched, which is now simply the office's signal. `lot-scan/appsscript.json` drops the Drive v2 advanced service (no remaining `Drive.` call — `uploadPhotoOnly`/`saveBlobToDrive_` use plain `DriveApp`)
- Lot Scanner: order-session capture flow (tap-loop camera, background upload queue, incremental draft commits) replaces the real-time OCR flow; gallery upload folded into orders
- VIN Inbox: groups by order batch (`batchId`) instead of dealer name, with an OCR-progress "N/M read" chip while a batch is still processing; legacy/blank-batch rows still fall back to one group per dealer; groups sort newest-first (T2.1)
- Lot Scanner server: barcode VINs ride batch commits (skip OCR); correction extracted to correctSubmissionVin; synchronous-OCR capture path retired behind a deprecation stub
- Unified pills sized up (11px, 6px/14px padding) per design review
- Run Order migrated to the unified component layer (buttons, pills, table, focus ring)
- Normalization + Field Codes migrated to the unified component layer
- Utilities migrated to the unified component layer
- Import migrated to the unified component layer (btn-secondary → btn-ghost)
- Data Sources migrated to the unified component layer
- Pipedrive Settings migrated to the unified component layer
- End of Month migrated to the unified component layer (buttons, status tags, dead hex fallbacks removed)
- VIN Inbox migrated to the unified component layer (badges → tags)
- Dealer Rules migrated to the unified component layer (pills, buttons)
- Home + VIN Logs migrated to the unified component layer
- Encarta table-header override retargeted to .table-u; theme coupling audit clean

### Removed
- Home's generic DASHBOARD-sheet renderer (`renderDashboardRows_`, `.dash-*` dialect) and the on-screen Runs by Type / Runs by Dealer / raw Inventory Snapshot tables; server `getDashboardView` retired (zero callers). The DASHBOARD sheet itself and `refreshDashboard_` remain — human-viewable in Sheets and the source `getInventorySnapshot` slices
- VIN Inbox "Processed" button + `viProcess` — no place in the workflow (batches exit via Discard; the create-order flow prompts a batch discard after the order finalizes). Server `updateVinSubmissionStatus` keeps the 'processed' status (shared plumbing, and `sweepLotPhotos` still recognizes historical rows)
- Auto-Hide Navigation — UI Settings toggle, `ui_autohide` UserProperty read/write, `UiPrefs.autohide`/`setAutohide` (both SharedUtils copies), and the `initialAutohide` template var (removed from `openApp`/`doGet` and App.html together — a one-sided removal 500s the app)
- Orphaned `data-shell="right-rail"` CSS block in App.html — no registry theme ever declared it
- Lot Scanner Drafts — OCR-era field UI (`processing…` badge, `(no VIN)` text, per-row Re-check + now-dead server `correctSubmissionVin`/`updateLotSubmissionVin_`) and the debug + live-camera PROBE dev panels (incl. `.ls-diag` CSS). A no-barcode draft row now shows a neutral "office reads VIN" badge

### Fixed
- `getInventorySnapshot` banner detection now prefix-matches — the live DASHBOARD banner reads "INVENTORY SNAPSHOT — BREAKDOWN BY LOCATION" and the exact-match check returned "No snapshot" even right after an import (the sheet-resident banner text can drift; verified against the live sheet)
- `getDealerPhotoFolder_` no longer creates duplicate dealer subfolders when a dealer's first-ever photos upload through the 5-wide parallel pool (field-hit on the fresh Shared-Drive folder: one order → two subfolders). Fast path stays lock-free; the create path takes a script lock and re-checks inside it. An upload never fails on lock contention (best-effort wait)
- `runInboxOcr` re-verifies each row's ID immediately before writing OCR results and re-locates by ID (or skips) if it moved — the scanner project `deleteRow`s on discard and LockService can't coordinate across the two scripts, so a crew discard mid-OCR-run shifted rows under the office's cached row numbers (whole-branch review finding; same corruption class as the old trigger's orphan rows)
- Lot Scanner: finish guards failed photos (no silent drop); idempotent batch commits (no dupes on retry); OCR-trigger + send failures surfaced; cancellable finish
- Targeting-rule AND/OR chip suppressed by the select enhancer (data-no-cs); danger-row tint no longer lost on hover
- Norm + Field Codes tables no longer flush against the page edge
- VIN Inbox view root now declares `background: var(--bg)` — same dark-theme white-guard trap as the EOM view (July 2026), caught this time during migration instead of after ship
- Import's `.bulk-bar` buttons now covered by the flex-shrink survivor rule (previously only `.modal-footer button` was, so bulk-action buttons could compress next to the flex:1 status label)
- UI Settings view root now declares `background: var(--bg)` — same dark-theme white-guard trap as the EOM/VIN Inbox views
- `.pill` and `.btn-primary`/`.btn-secondary`/`.btn-ghost`/`.btn-danger` padding now `!important` — Dealer Rules' type/status pills rendered with no padding because `#view-rules * { padding:0 }` (specificity 1,0,0) beat the unscoped component rule (0,1,0); same reset also silently flattened buttons in Import and Pipedrive Settings

### Fixed — EOM view white background in dark themes _(branch `feature/eom-billing-report`)_
- `#view-end-of-month` now declares `background: var(--bg)` like every other migrated view root. It only overrode `color`, so the SharedUtils **hardcoded-white `.view` readability guard** (interim rule for unmigrated views; ID-scoped roots are meant to out-specify it) kept painting the whole view canvas white in every dark theme while the tokened panels/text went dark. Root-caused via a local headless harness (computed-style dump: `#view-end-of-month = rgb(255,255,255)` with `--bg: #121214`); trap added to CLAUDE.md.

### Added — In-app EOM view: live "Current — EOM Merge" section _(branch `feature/eom-billing-report`)_
- The main app's **Reports** card gains a **Current (Live)** row — **View** pulls the EOM Merge stage straight from Pipedrive (new `getEomCurrentReport`, mirroring the standalone viewer's `getCurrentReport`: read-only GETs, writes nothing, same `{group, meta}` shape) and renders it through the shared renderer like an archived month, with a **Refresh** button in the viewer bar (shown only for the live view). Hidden until Pipedrive is connected. `eomvView`/`eomvViewCurrent` share the new `eomvOpenViewer_` shell helper.
- **Dark-mode audit:** built a local static harness (App.html + fragments assembled, `google.script.run` stubbed, headless Chrome screenshots) — the EOM view and the in-app report viewer render fully dark at HEAD; no hardcoded white backgrounds found (the only literal colors are theme-scoped encarta/luna rules and `color:#fff` on the accent button).

### Added — EOM viewer: pre/post-tax totals _(branch `feature/eom-billing-report`)_
- **Deal lines now carry the tax rate.** `eomGroupForReport_` (both copies — main `Code.gs` + `eom-viewer/Code.gs`) adds `tax` to each deal line: the line's actual Pipedrive Tax % (`deal_tax_percent`), falling back to the product catalog rate (`product_tax_percent`) for deals pushed before the June 30 line-tax fix. Both were already in the flat `EOM_COLUMNS` rows; they just weren't surfaced to the renderer.
- **Renderer shows pre- and post-tax** (shared `EomReportRenderer.html`, both byte-copies): a new **Monthly summary** bar at the top of the report (orders · duplicates · pre-tax · w/ tax grand totals), the dealer-card **total** chip splits into **pre-tax** / **w/ tax** chips, and each deal header shows `pre-tax $X` over `$Y w/ tax`. Post-tax = each line's `sum × (1 + tax%/100)` **rounded to cents per line** (mirrors Pipedrive's math); deal pre-tax comes from the line sums so pre + tax always reconcile. **Graceful degrade:** report.json files generated before this change have no `tax` on lines — the renderer detects that and falls back to the old single-total display (no fake "w/ tax" echoing the pre-tax number).
- Not touched: the per-dealer **PDFs** and org-tab spreadsheets remain pre-tax only (deferred with the PDF fine-tuning pass), and the product-summary table still shows pre-tax line-sum totals.
- `docs/LEARNINGS.md` (~14k tokens) is no longer auto-loaded into every session via `@docs/LEARNINGS.md` — same treatment the Bridge doc got. It remains the full incident record, read on demand; the brain vault carries distilled copies (`03-Resources/google-apps-script/`, `03-Resources/google-sheets/`, `03-Resources/pipedrive/integration-notes`), with the few remaining gaps backfilled in the same change (iOS text-size-adjust + on-device computed-style debugging, longest-match-first generalization, the 7-site type-removal guard, model-vs-DOM serialization, and the GitHub-workflow note).
- CLAUDE.md gains a **"Recurring traps — always in context"** section — a deliberately generous one-line-per-incident distillation of LEARNINGS.md (GAS runtime, SPA/HtmlService, Sheets values, Pipedrive API, Git/clasp/MCP) so the highest-frequency gotchas still ride along every session.
- The Pipedrive invariants block was tightened (same rules, fewer words, pointer to the Bridge + brain notes) and the stale per-machine checkout path was corrected. Net effect ≈ **80% smaller auto-loaded context** for this project.

### Added — Standalone EOM viewer: live "Current — EOM Merge" section _(branch `feature/eom-billing-report`)_
- The viewer sidebar gains a **Current** section (above a new **Published** label) with one item: **EOM Merge — live**. Clicking it pulls the EOM Merge stage (**stage 44**, `CURRENT_STAGE_ID` in `eom-viewer/Code.gs`) straight from Pipedrive and renders it through the same shared renderer as an archived month — so mid-month you can spot deals parked in EOM Merge that should be billed immediately. Toolbar shows the pull time + a **Refresh** button; an empty stage says so plainly — naming the **actual stage** it pulled (a `/stages/{id}` echo, so a wrong `CURRENT_STAGE_ID` is visible, not silent), and a `/users/me` probe surfaces rejected credentials as an error instead of a false "no deals"; a missing OAuth grant (owner never re-authorized after `UrlFetchApp` was added) gets its own distinct error. Nothing is written anywhere (no Drive file, no index row).
- **Security model change (deliberate):** the viewer's public surface grows from three to **exactly four** functions (`getCurrentReport` added) — a **read-only** Pipedrive pull (GETs only) of the same data class the published reports already expose; the token is never returned. The viewer project now needs its own `PD_API_TOKEN` + `PD_COMPANY_DOMAIN` in **Script Properties** (never repo/sheet); the Current section stays hidden until they're set (`getViewerBootstrap` returns a `current` boolean only). First `UrlFetchApp` use adds the external-request scope → re-authorize at next deploy.
- The row/group pipeline (`EOM_COLUMNS`, `eomCleanHtml_`, `eomGroupForReport_`, …) is a **verbatim copy** of the main app's Section 33 (keep in sync); the fetch layer (`cv*`) is viewer-local, never-throw, with the same batched `fetchAll` product read. Two deviations in `cvBuildRows_`: `dupKey` is a parameter, and pipeline/stage names are `''` (the renderer never reads them; saves two API calls per pull).

### Added — EOM viewer: per-dealer "Expand all" / "Collapse all" deals buttons _(branch `feature/eom-billing-report`)_
- Each dealer card's **Deals** section header gains two pill buttons — **Expand all** and **Collapse all** (shared renderer `EomReportRenderer.html`, both byte-copies) — that open/close every deal `<details>` in that card. Rendered only when the card has deals.

### Changed — EOM viewer refinements: merged-by-default contacts, themed viewers, wide layout _(branch `feature/eom-billing-report`)_
- **Per-contact split is now a toggleable option, OFF by default, everywhere.** ⚠ **Default flip:** the next generation produces **merged** PDFs (one continuous flow per dealer, no per-contact pages) and org tabs (no contact section headers) unless the new default-off **split** checkbox is ticked — tell the invoice team. The contact instead rides on every deal: `· Contact: X` in the PDF band meta, a `Contact` column on the tab's deal rows, and on each deal card in the viewers. Both interactive viewers get a **live "Split by contact" toggle** (initialized from the new `meta.splitContacts`). **Merge-at-consumption invariant:** `report.json` keeps the split shape forever (old/published files stay valid) — merging happens in the renderer (`eomrMergeGroup_`) and, for PDFs/tabs, in the new `eomMergeContactGroups_` (exact mirrors; parity-tested). `generateEomReport` gains the 5th `splitContacts` arg; `eomGroupForReport_` deals now carry `contact`.
- **Deal cards reworked** (shared renderer, both byte-copies): the deal title is no longer a hyperlink — a **"Go to deal ↗" button** sits in the expanded meta row instead, so the collapsed header is purely click-to-expand; the expand caret moved to the **left** and grew to 15px with a hover affordance; the meta fields are now **labeled** (`Owner:` — the Pipedrive deal owner; `Created:` — the deal's add date) in both the viewers and the PDF band.
- **Dealer cards are collapsible too** — each dealer (or contact, in split mode) is now a **collapsed-by-default** `<details>` card whose header shows the dealer name + the **orders / duplicates / total** chips; expanding reveals the product summary and the deals. The per-card "EOM — month" line moved out (both viewers already title the page with the month).
- **Theme system in both viewers.** The renderer's CSS switched to **design tokens with hex fallbacks**, so the in-app report now follows the app theme. The standalone `eom-viewer/` gets a **palette-only theme picker** (Light/Dark/Midnight HC/Encarta/Sage/Slate/Gruvbox — a deliberate hand-synced copy of the SharedUtils token blocks; structural themes don't port) persisted in **localStorage** with a pre-paint restore — **zero new server functions** (the 3-function public surface is a security boundary). Its navy header became themed neutral chrome.
- **Wide-window layout.** The in-app report viewer escapes the 760px form cap while open (`.eom-wide` → `--measure-data` 1680px); the standalone viewer's cap rose 1120px → 1680px; line-item description width 460px → `70ch`. `eomPdfSpike` now renders the merged default (kept for the still-deferred PDF fine-tuning pass).

### Added — EOM report viewer: in-app review + standalone published-only viewer _(branch `feature/eom-billing-report`)_
- **In-app review + publish gate.** The End of Month view gains a **Reports** archive (per month: scope/stage, counts, generated/published dates, status chip). **View** renders the report interactively — per org → per contact, product summary always visible, each deal a **collapsible** `<details>` (collapsed by default) — from a **`report.json`** (grouped data + meta, no flat rows) written to the month folder at generation. **Finalize & publish** snapshots `report.json` → **`published.json`** (a separate file) and flips the report's index row to `published`. New shared fragment **`EomReportRenderer.html`** (`eomrRenderReport`, `eomr*` prefix) renders in both the main app and the standalone viewer.
- **Standalone viewer module `eom-viewer/`.** A **separate Apps Script web app** (own `.clasp.json`, excluded from the main push via `.claspignore`) that the invoice team bookmarks at one `/exec` URL — deployed **`executeAs: USER_DEPLOYING` + `access: DOMAIN`**, so they need **zero** access to the app, sheets, or Drive files. It lists **published months only** and renders the byte-copied `EomReportRenderer`. Connected by one id (the new `SF_EOM_REPORTS` index). **Security model:** the public surface is **exactly** `doGet` / `getViewerBootstrap` / `getReportJson` (execute-as-owner means any domain user could call any public function with the owner's authority) — both data endpoints return **published rows + display fields only** (never file ids or Drive/spreadsheet URLs); optional `EOM_VIEWER_ALLOWED` email allowlist. Deploy runbook in the module header (use `/exec`, update via *edit deployment → new version* to keep the URL).
- **Index store `SF_EOM_REPORTS`** (one-time `setupEomReportsIndex()`; its own spreadsheet — SF_SYSTEM_MASTER never written): one row per month, **all-text** (`@`-format + `String()` writes) to dodge the Sheets date-parse + `google.script.run` Date-serialization traps. **Snapshot invariant:** re-generating a month (incl. test-stage runs) overwrites `report.json` and reverts the row to `generated`, but **never touches** the published snapshot — so what the invoice team sees only changes on a deliberate re-finalize. `getEomBootstrap` returns the reports list even when Pipedrive is disconnected (viewing needs no Pipedrive).
- **Removed** the temporary `eomFillProbe` (its job — settling the PDF fill mechanism — is done; `eomPdfSpike` retained until PDF layout fine-tuning). **No new OAuth scopes in the main project** (the viewer project has its own consent).

### Added — EOM report: per-dealer PDFs, readability-first _(branch `feature/eom-billing-report`)_
- Alongside the spreadsheet, each run now emits **one PDF per dealer** (each contact on its own page) into a per-month folder `"<Month Year> EOM Reports"` nested under the *EOM Reports* folder — which now holds the spreadsheet **and** the PDFs together. New **Report month** picker (defaults to the current month) names the folder, the PDF files, and each page header. Shared **`eomGroupForReport_`** feeds both the spreadsheet tabs and the PDFs; `eomPutFile_` gives **replace-by-name** so re-runs stay clean.
- **PDF engine — the hard part, settled by a live probe.** Rendered via the raw `Utilities.newBlob(html,'text/html').getAs('application/pdf')` converter (for its crisp typography), but a one-page **fill probe** proved that converter paints **no background fills** behind HTML text — CSS `background`, inline `background-color`, and the `bgcolor` attribute **all fail**; only borders, text, and **images** render. So the light-blue deal-header bands and stat pills are drawn as **baked SVG images** (`eomSvgUri_`: band rectangle + title/value/meta text baked in, wrapped in the deal's Pipedrive link); line items stay selectable HTML text. (An HTML→Google-Doc→PDF path was tried and rejected — the Docs importer imposed its own boxy table borders.) Layout = the approved "deal cards + blue header band": product-summary table + per-deal cards, long descriptions as muted wrapped text.
- **No new OAuth scopes.** _Deferred: fine-tuning the PDF layout against real data._

### Added — End of Month billing report (Pipedrive → per-org report file) _(branch `feature/eom-billing-report`)_
- **Ported the standalone "EOM / BillingDashboard" Apps Script tool into the main app** as a first-class **End of Month** view. It pulls billing deals from Pipedrive **by pipeline stage** (staff move deals into the *EOM Merge* stage, then run — not date-based), flattens each deal's products, and builds a **new dated spreadsheet** with a flat 28-col audit tab plus **one formatted tab per organization** (product summary + per-deal line-item breakdown with a Pipedrive hyperlink). New `Code.gs` **Section 33** + `ViewEndOfMonth.html` + `App.html` wiring.
  - **Reuses the Section-31 Pipedrive layer** — auth via `pdGetSecrets_` (no separate token; the standalone's hardcoded `PIPEDRIVE_API_TOKEN`/domain are dropped), deal listing + pagination via `pdListAllV1_`, and the *Duplicates* custom-field key via `pdListDealFields_`. The one net-new fetch is **`pdEomFetchProductsForDeals_`** — a **parallel `UrlFetchApp.fetchAll`** over `/deals/{id}/products` (batched 100), since sequential per-deal reads over hundreds of deals would blow the 6-min cap.
  - **Run-time stage picker.** The view lists the billing pipeline's stages (`pdEomListStages_`) and defaults to the configured EOM stage, but you can point the pull at **any stage** (e.g. *EOM Exported*) — so a report can be re-run for testing after the real deals have left *EOM Merge*. A **Full billing (all open)** scope is also available. Pipeline/stage defaults (4 / 44) are configurable in the view's **Report settings** panel (`getEomBootstrap` / `saveEomSettings`, persisted in `PIPEDRIVE_SETTINGS`).
  - **Output is a NEW dated Google Sheet per run** (created in an auto-made *EOM Reports* Drive folder, returns a shareable link) — **nothing is ever written to SF_SYSTEM_MASTER**, and the standalone's destructive "delete all other tabs" step is gone (a fresh file starts empty). Per-org tabs are built **from the in-memory rows**, not by re-reading the sheet — avoiding the `getSheets()[0]` / `openById`-vs-`getActive` cache traps.
  - **Porting hardening:** `TextCleaner`/`RowBuilder` ported verbatim (`eomCleanHtml_`, `eomBuildRows_` — a product-less deal still emits one row); per-org tabs write **one bulk `setValues` + block formatting** instead of the standalone's thousands of per-cell calls; `pdEomFetchProductsForDeals_` never lets one deal's failure abort the batch. Long runs show a live progress bar (`generateEomReport` emits progress to ScriptProperties; the view polls `getEomProgress`), and `AppBusy` blocks a report while a run/import is active.
  - **No new OAuth scopes** (`SpreadsheetApp`/`DriveApp`/`UrlFetchApp` already granted). _Deferred (per plan): the readability redesign of the per-org tabs (tall wrapped rows / large text blocks) and any charts/visuals — this pass ports the fundamentals and a usable UI._

### Fixed — Pipedrive: product **tax** now pushed onto deal line items _(branch `feature/lot-vin-scanner`)_
- Products pushed to a Pipedrive deal were attached at **0% tax** regardless of the product's catalog **Tax %**, so the deal's "Total with tax" was wrong (a manually-added product showed the correct tax; a system-pushed one didn't). Root cause: Pipedrive's API does **not** auto-copy a product's catalog tax onto a deal line — only the UI does — and the push never sent `tax`/`tax_method`. Backend `Code.gs` only; no UI/schema change.
  - **`pdListProducts_`** now captures **`tax: Number(p.tax) || 0`** from the catalog product (both the v1 branch and the org-scoped v2 branch — verified live that v2 `/products` returns `tax`).
  - **`buildLineItems_`** sets each line's `tax` from its catalog product; **`mergeLineItems_`** preserves `tax` when collapsing source-split lines.
  - **`pdAttachProducts_`** and **`pdAddDealProduct_`** now send **`tax` + `tax_method: 'exclusive'`** on `POST /deals/{id}/products` — mirroring a manual add (SilverFox never includes tax in the product price, so the method is always exclusive). Verified live: a pushed line now matches a manual one (e.g. a 9.679% product yields the correct "Total with tax"); products with a 0% catalog rate (Design, Installation) are unaffected. Applies to both **create** (New Deal) and **link** (existing/manually-created) pushes.
  - **Known limitation:** the attach dup-guard still skips a product **already on the deal**, so a line attached by a *prior* push at 0% is not retro-updated on a re-push — only newly-attached lines get tax. (A product-less manually-created deal — the common case — attaches everything fresh, so it's correct.)

### Changed — docs: prune + restructure `CLAUDE.md` against memory best-practices _(no code change)_
- Audited `CLAUDE.md` against Anthropic's Claude Code memory guidance and trimmed it **372 → 203 lines** — no invariant lost. **All 28 invariants preserved**: kept as terse, header-grouped stubs (Schemas / Types / Run pipeline / Scraper / Pipedrive); mechanism/history now points to the Bridge doc instead of being restated inline. Stripped the pervasive bold emphasis down to the few load-bearing words (it had lost all signal). The only content dropped from always-on is the one-time **bulk dealer→org linker** setup note (a tool, not a break-risk rule — detail lives in the Bridge doc).
- **Backlog moved to the brain.** The "Current to-do" section (stale "verified June 10", a `✅ DONE` item) is replaced by a pointer to the vault's `01-Projects/gas-ops-v2/open-issues.md`, which is fresher/richer (adds a Phase 1A hardening backlog). Also removed the resolved June-18 `scraper_location_name` drift-audit narrative (live rule retained; history → brain/Bridge).
- **Promoted a rule up from the brain:** added the **"Pipedrive is read-only for Claude"** working agreement (Claude writes the API scripts, Nick runs them) — an always-on safety rule after a debug probe created a scratch deal that had to be deleted by hand.
- **Accuracy fixes:** `SF_DEALER_CONFIG` tab list now includes `PIPEDRIVE`/`PIPEDRIVE_SETTINGS`; `SF_UNIVERSAL_TEMPLATE` ORDERMATCH corrected `A–T` → `A–U` (it contradicted the `PRICE_TAGLINE = col 21 (U)` invariant). Reference-docs section now lists the brain vault as a first-class tier.

### Changed — Lot Scanner v2: guided real-time capture + async batch + Drafts _(branch `feature/lot-vin-scanner`)_
- **Two capture paths.** 📷 **Scan a VIN** (camera) is now a **guided real-time flow**: each shot decodes/OCRs immediately and shows one outcome — ✅ *in inventory* (Take another), ⚠️ *valid VIN not in this dealer's stock* (verify against the photo → **Add & next** / **Retake**), or ❌ *no VIN read* (**Retake**; on the **2nd consecutive fail** also **Submit photo only** for the office to transcribe). Retake calls **`discardSubmission`** (delete the row + trash its photo). ⬆ **Upload batch** (gallery) is now fully asynchronous.
- **Asynchronous batch that survives the app closing.** **`queuePhoto`** saves each photo + a `queued` draft row **with no OCR** (fast — the crew never waits), then **`ensureOcrTrigger`** installs a **per-user time-driven trigger** (**`drainOcrQueue`**, runs as that user so OCR uses their own quota) that processes the queue in the background (~8/run, rate-limit-bounded) and, when the user's queue drains, **emails them a summary** (`MailApp`) and **self-deletes**. Closing the app mid-processing loses nothing.
- **Drafts (field) + Inbox (office) — "Both".** New **Drafts** tab in the scanner (**`getMyDrafts`**, grouped by batch; per-row correct-VIN / View-Photo / Discard + a per-batch **Send to office** → **`sendDraftBatch`**). The main-app **VIN Inbox** now shows **open** work (`draft` + `submitted`) grouped by dealer, with a **"draft — not yet sent"** badge and a **"still processing…"** state for queued rows (actions appear once OCR finishes).
- **Store:** `SF_LOT_SUBMISSIONS` gains **`batch_id`** + **`ocr_state`** (`queued`|`done`|`failed`) — append-only; an existing sheet's header auto-upgrades on next access. The `status` lifecycle is now **`draft` | `submitted` | `processed` | `discarded`** (was `pending`/`processed`/`discarded`). `getVinSubmissions()` with no filter returns open work; `updateVinSubmissionStatus` allow-list updated. Higher-res capture (3000px) + ZXing barcode-first retained; the temporary on-card **debug** panel is kept for now.
- **New OAuth scopes** (Mail + Script triggers) — the scanner re-prompts for authorization on next open.
- **Faster, loss-proof batch upload + in-app photo viewing.** Batch upload is now **parallel uploads (5 at once, Drive only via `uploadPhotoOnly`) + a serial, chunked row committer (`commitQueuedBatch`, 10/call)** — fast *and* lossless (the earlier parallel-append approach lost rows: opening the sheet outside the lock cached a stale last-row so concurrent `appendRow`s overwrote each other; rows are now written by a single execution). Photos view **in-app** — the camera result shows the local capture inline instantly (VIN + photo together); Drafts have a **📷 Show/hide photo** that fetches the bytes server-side (`getPhotoDataUrl`, reliable in the web-app sandbox where a `drive.google.com/thumbnail` URL won't load) and shows them inline; tap to enlarge in a lightbox.
- **Drafts are collapsible.** Each batch is a `<details>` group (**collapsed by default**) headed by dealer · timestamp · count · short batch id, with **Send to office** and **Delete batch** (`discardBatch` — trashes every photo + removes every row).

### Added — On-lot VIN Photo Scanner (standalone app) + VIN Inbox _(branch `feature/lot-vin-scanner`)_
- **New standalone "Lot Scanner" web app** (`lot-scan/` — a **separate** Apps Script project with its own deployment + scopes; not bound to a sheet). A second way to build vehicle lists besides CAO: on a dealer lot, the user picks a dealer, photographs a VIN (dash / Monroney / windshield tag), and the tool OCRs the VIN, validates it, matches it against that dealer's live inventory, shows **found / not-found** on the spot, and submits the photo + VIN. Mobile-first (reuses a copied `SharedUtils.html` for tokens / `toast` / `escHtml` / `CustomSelect` / the ≤760px mobile layer). Files: `lot-scan/appsscript.json` (web app `executeAs: USER_ACCESSING`, `access: DOMAIN`; Advanced **Drive** service v2), `lot-scan/Code.gs`, `lot-scan/Capture.html`, `lot-scan/SharedUtils.html`.
  - **Native Google Drive OCR** — `extractVinFromImage_(blob)` inserts the photo as a Google Doc with `{ocr:true, ocrLanguage:'en'}` (Advanced Drive Service v2), reads the OCR'd text via `DocumentApp`, and **always trashes the temp Doc**. Free, no API key, no third-party egress. Never throws → `''` on failure (degrades to manual entry).
  - **VIN validation (ISO 3779)** — pure `isValidVin_` (`^[A-HJ-NPR-Z0-9]{17}$`, no I/O/Q, **plus the check digit** at position 9) + `vinCheckDigit_`. `extractVinCandidates_` forces the VIN-illegal O/I/Q to `0/1/0`, prefers **exact-valid 17-char tokens**, and only falls back to de-spaced sliding-window + bounded single-char OCR-confusion repair (0↔O, 8↔B, 5↔S, 2↔Z, …) when no clean token matches — the check digit gates every candidate. (10-assertion Node self-check.)
  - **Dealer-scoped match** — reuses the main app's logic (copied `getDealerScraperData_` + `buildVinDataMap_`) to look the VIN up in the chosen dealer's SCRAPERDATA; server re-match is authoritative.
  - **Client-side barcode / Data-Matrix decode first** (ZXing, loaded from CDN). The CDK windshield labels carry the VIN in a barcode (a 2D Data Matrix, or a 1D Code 39/128) — `vpTryBarcode` decodes 2D formats first, then 1D, and validates each decode as a real 17-char VIN (a stock-number barcode fails the check digit → ignored), passing a hit straight to `submitVinPhoto` (server skips OCR). Far more reliable than OCR'ing small/rotated printed VINs; falls back to OCR (and the user can always correct) when nothing decodes.
  - **Client-side image downscale** (canvas, longest edge ~3000px, JPEG q0.85) before base64 — large enough for OCR to read the small printed VIN, small enough for the `google.script.run` payload. (The first cut used 1280/0.7, which shrank the VIN below OCR legibility — the cause of the initial "didn't find the VIN" misses.)
  - **Submissions store** — one row per photo to a dedicated **`SF_LOT_SUBMISSIONS`** sheet (auto-created; flat/long, SQL-portable) + photos to a per-dealer subfolder under a dedicated Drive folder. `submitVinPhoto(dealerKey, base64, correctedVin, submissionId)` orchestrates (correction path updates the existing row, no new photo). `setupLotScannerResources()` (run once) creates the sheet + folder and logs their IDs.
- **New "VIN Inbox" view in the main app** (`ViewVinInbox.html` + `App.html` wiring + Code.gs **Section 32**). The office processor reviews pending submissions **grouped by dealer**, each with the photo thumbnail, the OCR'd VIN (editable → re-validate + re-match), valid/matched badges + matched vehicle data, and **Processed / Discard** actions; a per-dealer **"Copy valid VINs"** button copies the list to paste into Run Order. Server: `getVinSubmissions(filter)` + `updateVinSubmissionStatus(id, status, correctedVin)` (status allow-listed `pending|processed|discarded`), reading the shared `SF_LOT_SUBMISSIONS` sheet.
- **Connection model:** the two apps share **no runtime code** — the scanner connects to the main system purely by **shared data IDs** (reads `SF_DEALER_CONFIG` + `SF_SYSTEM_MASTER` SCRAPERDATA; writes `SF_LOT_SUBMISSIONS` + the photos folder, which the inbox reads). Reused helpers are deliberate copies. _Setup pending before live use: run `setupLotScannerResources()`, paste `LOT_SUBMISSIONS_SHEET_ID` into both projects, enable the Drive API service, deploy the scanner web app, and grant the lot crew access._

### Added — UI Settings (per-user nav layout) + mobile/responsive layer _(branch `feature/theme-presets`)_
- **New "UI Settings" view** (`ViewUiSettings.html`, under **System Settings**) — per-user display preferences that layer **on top of** the chosen theme. A **nav-layout** chooser (cards): **Left Sidebar**, **Top Rail**, **Bottom Rail** (new), **Windows Start Menu** (new — a bottom taskbar with a Start button that pops up the nav panel, plus a live taskbar clock `#startClock`), plus an **autohide** toggle (collapses the left sidebar to an icon rail that expands on hover). Persisted per Google account by **`getUiPrefs()` / `saveUiPref(key, value)`** (Code.gs, UserProperties `ui_nav_layout` / `ui_autohide`; fail-safe — an off-list nav layout falls back to `auto`, validated against `UI_NAV_LAYOUTS_`). Injected **pre-paint** as `window.UI_PREFS` and applied by **`UiPrefs.applyOverrides()`** (SharedUtils) — a user's explicit pick **overrides** the theme's `data-shell`; **"Auto" follows the theme**. The System Settings nav-group is now **collapsed by default**.
- **Mobile / responsive layer** (`SharedUtils.html` + `App.html` + the gridified views). **`data-viewport="mobile"` is set automatically at ≤760px** (a head pre-paint check + a `matchMedia` listener in SharedUtils). On mobile **every theme uses the Start-Menu taskbar nav** (`applyOverrides` forces `data-shell="start-menu"` regardless of theme/pref); the grid-skeleton views **stack to one column**; per-view mobile rules handle dense tables — generic tables get **horizontal scroll**, the Pipedrive **product table reflows to one card per type** (each `<td data-label>` renders its label via CSS `::before`), VIN Logs / Normalization tables **shrink-to-fit** instead of scroll, and Run Order switches to **full-page scroll** with a min-height VIN box. The **linear views (Norm, Field Codes) are deliberately not structurally gridified** (their table height is JS-computed). The Start-Menu taskbar uses a **48px left / 28px right inset** to clear iPhone rounded-corner curves (the Start button sits deepest in the bottom-left curve; the clock clears at 28px) — the `viewport-fit=cover` / `env(safe-area-inset-*)` approach was tried and reverted.

### Added — Theme system: reusable structural axes + grid-area page skeletons + "Windows XP" _(branch `feature/theme-presets`)_
- **Composable structural axes** so a single curated theme can reshape *layout and elements*, not just colors — while the picker stays one-pick ("Encarta is one complete idea"). A theme declares optional axes in its `Theme.themes` metadata; `Theme.apply()`/`reflectAxes()` write each onto `<html>` as `data-<axis>` (an axis the theme omits is **removed** → its default). Reflected **pre-paint** (the `reflectAxes(current())` call sits at the end of the `SharedUtils` script, which `App.html` includes before `#appRoot`), so the chosen layout paints right the first time — no flash. Theming is still a **single curated `data-theme` pick** persisted as a slug (`saveThemePreference`/`isThemeSlug_` unchanged). Axes (`Theme._axes`):
  - **`data-shell`** (App.html) — shell structure: `top-rail` (nav as a top bar) / `right-rail` (nav on the right). Default = left sidebar. The existing Top-Rail / Dark-Dense structural CSS was re-keyed off `data-theme` onto `data-shell`, so any palette composes any layout with zero duplicated CSS.
  - **`data-density`** (SharedUtils tokens) — `compact` / `spacious` type + space scale. Default = comfortable. (Dark-Dense's dense scale moved here.)
  - **`data-shape`** (SharedUtils tokens) — `sharp` (0-radius) / `flat` (no drop shadows). Default = rounded.
  - **`data-nav`** (App.html) — `icons` (collapse the rail to an icon strip that expands on hover/focus). Default = labeled.
  - **`data-arrange`** (NEW) — page-layout axis driving the grid-area skeletons below. **Deliberately not `data-layout`** — that attribute already lives on each `.view` root for content-width tiers and must not be overloaded.
- **Grid-area page skeletons (the big new capability).** Four views' top-level layout parents became `display:grid` with named `grid-template-areas`, each section assigned a `grid-area`, the **default template reproducing today's layout exactly (a no-op)**: ViewRun `.rv-body` (`"vin table rail"`), ViewHome `.home-scroll` (`"status" "wflabel" "cards" "dashhead" "dash"`), ViewVinLog `.layout` (`"main side"`), ViewImport `.import-cols` (`"left right"`). A theme rearranges a page by redefining the template under `[data-arrange="…"] #view-xxx` (reusable) or `[data-theme="id"] #view-xxx` (bespoke) — **no markup or JS change, backend-safe** (axes/skeletons never touch view JS or a `google.script.run` call). ViewPipedriveSettings `.ps-scroll` stays flex-column (dynamic card count, `order:`-able). The linear / height-calc-JS views (ViewNorm, ViewUtilities, ViewFieldCodes) are deliberately **not** gridified.
- **Element-size tokens** in base `:root`, defaulted to the current literals (no-op), so a theme can resize chrome with zero per-view CSS: `--shell-header-h:46px`, `--shell-sidebar-w:210px`, `--brand-fs:17px`, `--rv-vin-w:250px`, `--rv-rail-w:280px` (reuses `--col-form-side`) — now referenced by `#appHeader` / `#sidebar` / `.app-brand` / `.rv-body`. Some hardcoded font-sizes swapped to `--fs-*`.
- **New theme "Windows XP" (`luna`)** — the first showcase of the approach (metadata `{ arrange: 'desktop' }`, no shell axis). Palette/token block in `SharedUtils` (Luna blue `#316ac5` accent, beige `#ece9d8` controls, white client, Tahoma/Trebuchet system fonts — no CDN webfont, 3px radius, `--shell-header-h:30px`); a window-frame chrome block in `App.html` (blue-gradient title-bar header, rounded-top white window over a blue desktop `#appRoot`, Luna side panel, **green** taskbar footer, glossy grey buttons, sunken white inputs); added to the dark-logo lightening list. It visibly **rearranges two pages**: Home becomes a grid of large captioned desktop icons over the blue desktop (theme-agnostic `data-arrange="desktop"` block, reusable by any future desktop theme), and the Run Order rail moves to the **left** as an Explorer-style task pane — one line via the grid skeleton (`grid-template-areas:"rail table vin"`).

### Changed — CustomSelect dropdown hardening (mobile + cross-view) _(branch `feature/theme-presets`)_
- **`CustomSelect.enhance()` survives a select's inline `onchange` firing on iOS re-parent** (`SharedUtils.html`). Wrapping a `<select>` (insert wrapper → move the select into it) makes iOS Safari fire a `change` event mid-surgery, so any inline `onchange` ran before its view was initialized and threw → the enhancement reverted (the select stayed native on mobile). Fix: **detach the inline `onchange` attribute during the DOM move and restore it afterward**.
- **Grow-flex selects now fill their slot.** A `flex:1` select's computed `flex-basis` resolves to `0%`; the enhancer had copied that onto the wrapper as `0 0 0%`, collapsing the control (the "smaller dropdown" / truncated "Running As" symptom). Now a positive `flex-grow` → `1 1 <basis|0%>` (grows), only a real fixed basis (e.g. `0 0 220px`, Norm's map picker) is copied as a fixed slot, everything else fills `100%`.
- **Selects are re-enhanced on view show** (`navTo`) so a select rendered while its view was hidden (measured 0px) is corrected when the view becomes visible.
- **Shared native `<select>` base style** added; **`text-size-adjust: 100%`** pinned (`:root`) to stop iOS font-boosting from scaling identical-px dropdown text differently per container.

### Fixed — dropdown option padding + Pipedrive Settings domain label _(branch `feature/theme-presets`)_
- **"Tiny dropdown options" root cause** (`SharedUtils.html`). Five view scopes carry a wildcard `#view-xxx * { padding:0 }` reset (specificity 1,0,0) that beat the injected widget's class rules (`.cs-btn` / `.cs-menu li`, specificity ≤ 0,1,1) and **zeroed the CustomSelect widget's padding** — so its options rendered cramped. The widget now sets its own padding with **`!important`** (button + menu items) so a host view's blanket reset can't flatten it.
- **Pipedrive Settings "(unknown domain)" label** — `getPipedriveSettingsBootstrap` now returns `domain` (from `getPipedriveStatus`), so the "Connected to <domain>" label resolves instead of showing "(unknown domain)".

### Removed — cut palette themes + Cyberpunk shell scaffolding + "Close SilverFox" buttons _(branch `feature/theme-presets`)_
- **9 themes from the experimental set were dropped**, leaving 9 + Luna = **10**: kept **Light, Dark, Top Rail, Top Rail — Dark, Midnight HC, Encarta, Sage, Gruvbox Rail, Slate — Icon Rail**; removed **Dark — Dense, Cyberpunk, Solarized Light, Sepia, Slate (standalone), Solarized Dark, Nord, Gruvbox (standalone), Rosé**. The Gruvbox/Slate palette token blocks **stay** (now reused only by the `-rail` / `-icons` composed variants — the standalone `data-theme` selectors were dropped). The **Cyberpunk** shell block and the **Rajdhani** Google Font were removed.
- **Non-functional "Close SilverFox" buttons removed** — the App sidebar nav item plus three view-footer instances. `exitApp` is **kept** (it still backs the Run Order **Cancel** button, which runs the AppGuards before closing).

### Added — Run Order + VIN Logs + Import Data enhancements _(branch `feature/run-order-vin-table`)_
- **Vehicle URL column in the Run Order live table** (`ViewRun.html`) — an 8th column ("Link") added to the VIN data table. Found rows with a valid URL render an anchor ("Vehicle URL") that opens the listing in a new tab; rows with a blank or `*` URL show "—"; not-found rows show "—". `buildVinDataMap_` now maps SCRAPERDATA col U (index 20) as `url` on each entry.
- **VIN list popup in VIN Logs** (`ViewVinLog.html`) — each run row now shows a **Show VINs** button in the VIN count cell (only when `producedVins` is available). Clicking it opens a fixed-position overlay popup displaying the VINs for that run in a monospace read-only textarea, with a **Copy** button (fills to "✓ Copied" then resets) and a **Close** button; clicking the backdrop also closes it. Uses integer-index handler (`showVinPopup(idx)`) for apostrophe-safe lookup. No DOM reflow — all existing run-flow elements are unaffected.
- **"Browse Current Data" button and Current Data panel** (`ViewImport.html`) — a "Browse Current Data" button added to the Import form footer opens a fourth panel (`#currentDataPanel`) showing the current SCRAPERDATA snapshot. The panel provides **Location** and **Type** filter dropdowns that re-fetch on change. Results render as a scrollable table (VIN · Stock · Type · Year · Make · Model · Trim · Status · Price · Location · Link) capped at 3000 rows with a count note when the cap is reached (SCRAPERDATA is the combined all-dealer feed, so the cap only bounds the unfiltered "All Locations" view — a single dealer is never truncated). The vehicle URL renders as a clickable "URL" link. Backed by the new **`getScraperDataPreview(locationFilter, typeFilter)`** server function (Code.gs), which reads SCRAPERDATA, applies the optional filters, and returns `{ rows, totalCount, cappedAt, locations, types }`. Non-fatal: the function wraps in try/catch and returns an empty result on any error.

### Changed — Run Order bypass-checkbox relocated; topbar restructured _(branch `feature/run-order-vin-table`)_
- **"Bypass filtering rules" checkbox moved out of the top control bar** into the VIN zone (below the textarea), so adding/removing the `splitDealRow` for billing-split dealers (e.g. Mercedes-Benz of Creve Coeur) no longer shifts the button positions. The topbar is now two flex children: `.rv-topbar-inputs` (the 4 input rows — user select, dealer, Deal ID, Split Deal ID) and `.rv-topbar-actions` (the 3 buttons — Pre-fill CAO, Run Dealer, Cancel), so the action column never moves regardless of how many input rows appear.

### Added — Run Order live VIN table + Transcription retirement _(branch `feature/run-order-vin-table`)_
- **`getDealerVinData(dealerKey)`** (new client-callable, Code.gs) — returns a `{ "<VIN-UPPER>": {year, make, model, type, stock, status} }` map scoped to the selected dealer's current SCRAPERDATA inventory. Built by the pure helper **`buildVinDataMap_(rows)`** (converts the 2D SCRAPERDATA row array into the keyed map). Reuses `getDealerConfig_` + `getDealerScraperData_`; fail-safe `{}` on any error so the Run Order screen always loads even if the inventory read fails.
- **Live inventory table in the Run Order screen** (`ViewRun.html`). After a dealer is selected, the map is fetched once and held client-side. As VINs are typed or pasted, a **debounced lookup** checks each line against the map and renders a **`Year · Make · Model · Type · Stock · VIN · Status`** table row per VIN. A VIN present in the dealer's inventory shows its data; a VIN absent from the dealer (but maybe valid elsewhere) shows a red **"not in this dealer"** row. A **count strip** ("N VINs · X found · Y not found") updates with every keystroke. No round-trip per keystroke — the full dealer map loads once on dealer select.
- **8-theme compatibility** — the table and reserved right rail use tokens-only styling (no hardcoded colors); verified to reskin across all 8 themes (Light, Dark, Dark — Dense, Top Rail, Top Rail — Dark, Midnight HC, Cyberpunk, Encarta). Explicit **Encarta** coverage added for the new elements (beveled table header, sunken cells, Explorer-selection row highlight, sharp 0-radius container).

### Changed — Run Order layout redesigned to Option B _(branch `feature/run-order-vin-table`)_
- **Top control bar + VIN box + live table + reserved right rail.** The Run Order view is reorganized: a single **top control bar** holds all controls (Running As · Dealer · Log Note · Deal ID · Split Deal ID · Bypass · Pre-fill CAO · Run); the body below splits into a **narrowed VIN textarea** (left) next to the **live inventory table** (center/left), with a **reserved right rail** (right) that houses all existing run-flow elements — CAO rejection summary, progress bar with step messages and timer, and finalization cards / Add-to-VIN-Log. All existing element ids are preserved; no server function signatures, run/CAO/finalize logic, or `filtering_rules` behavior changed — pure layout and table addition.

### Removed — Transcription page _(branch `feature/run-order-vin-table`)_
- **`ViewTranscription.html` retired** — the view file, its sidebar nav item, and its `<?!= include_() ?>` in `App.html` were removed. The live inventory table in the Run Order screen subsumes the Found/Not-Found check: the table is dealer-scoped (rather than global), answers the same question at the point of need (while composing an order), and requires no separate navigation step. The **TRANSCRIPTION sheet tab** in SF_SYSTEM_MASTER is retained (unchanged).
- **`getTranscriptionVins()`** removed from Code.gs — no longer called by any view.

### Added — Themed dropdown enhancer (custom `<select>`) _(branch `experimental-themes`)_
- **`CustomSelect`** (`SharedUtils.html` + one `start()` call in `App.html`) progressively enhances every native `<select>` app-wide into a themed button + `<ul role="listbox">` — the generalized form of the theme picker. **Why:** a native `<select>`'s open list is OS-rendered and **can't be themed in the Apps Script web-app sandbox** (the same root cause as the theme-picker bug), so native dropdowns read light-on-light in dark themes and can't take the Encarta bevels. Now the open list is real DOM under our CSS, themed in every theme.
- **Never breaks a select:** the native `<select>` stays the value source — each pick sets `select.value` + dispatches a bubbling `change`, so all existing `onchange` logic runs untouched; every enhancement is `try/catch`-wrapped and **reverts to fully-native on any error**; `data-no-cs` opt-out + a `window.CS_DISABLED` kill-switch. Auto-applies via a debounced `#appContent` MutationObserver (catches dynamically-rendered selects); per-select observers handle option repopulation; selects are looked up by id elsewhere so wrapping them is safe. Menu is `position:fixed` (escapes `overflow:auto` clipping). Themes for free via `.cs-btn`/`.cs-menu` tokens. Fixes after live testing: the menu's own internal scroll no longer closes it; the wrapper fills its container so a select enhanced while its view was hidden (measured 0px) no longer truncates ("Running As"); the open list grows to fit the longest option.

### Added — "Encarta" theme (Windows 95/98 + 90s multimedia) _(branch `experimental-themes`)_
- **The 9th theme** (`SharedUtils.html` token block + `App.html` shell + a Pixelify Sans webfont). Reframes the app as a Win95 application window: navy gradient **title-bar** header, raised gray **chrome** sidebar, sunken white **client** content area. Inverts every default the app leans on — rounded pills/cards → **sharp 3D bevels** (`outset` raised / `inset` sunken), soft shadows → hard bevels, accent-fill active states → **navy Explorer-selection bars**, smooth motion → **instant/no animation**; plus chunky beveled `::-webkit-scrollbar`s, dotted focus rectangles, Tahoma (system stand-in for MS Sans Serif) + a pixel wordmark, `color-scheme: light`. **Component overrides** (`!important`, the legitimate top layer of intent for a theme) square + bevel the cards (`.filter-card`/`.home-card`/`.ps-card`), pills (raised when off, pressed-in when active — keeps per-type colour), toggles, and all buttons. Pure presentation, fully reversible. Limits (a theme can't change markup/JS): no working min/max/close window-control widgets, no Start button/taskbar, no true TreeView nav; a few id-scoped per-view "primary" buttons keep their fill colour but still go sharp + beveled.

### Added — Config-driven field codes + a Field Codes manager screen _(branch `experimental-themes`)_
- **The field-code → ORDERMATCH-column mapping is now config-driven**, so adding a CSV field code no longer needs a `Code.gs` edit + `clasp push` (`Code.gs` + new `ViewFieldCodes.html` + `App.html`). Previously the mapping lived only in the hardcoded `FIELD_TO_COL` constant; a new field code's CSV **header** came from the schema but its **data column landed blank** because `buildCSVSheet_` reads the data via `FIELD_TO_COL[code]` — and the script never reads the ORDERMATCH headers at runtime. (This is exactly the `PRICE_MAINLINE` symptom.)
- **`getFieldToCol_()`** (new, cached per execution, **fail-safe**) clones the `FIELD_TO_COL` constant — the protected floor — then overlays the **FIELD_CODES tab's new `ordermatch_col` column** for any row with a valid 1–100 column. Wrapped so it can never throw into a run; with an empty/missing column the resolved map equals the constant **byte-identical**. `buildCSVSheet_` resolves it once and reads `fieldToCol[code]`. Mirrors the earlier `FILTER_FIELD_INDEX` → `getFilterFieldIndex_()` refactor; the `FILTER_FIELD_INDEX`-style constant stays as the documented fallback.
- **Client-callable CRUD:** `getFieldCodeMappings` (effective merged list with a built-in / custom / overridden source per code), `saveFieldCodeMapping(code, colInput, description)` (accepts a column **letter or number**; validates; auto-creates the `ordermatch_col` header), `deleteFieldCodeMapping` (removes a custom code or reverts a built-in override), `openFieldCodes` (Classic fallback). `normalizeOrderMatchCol_`/`ensureFieldCodesOrderMatchColumn_` helpers.
- **New "Field Codes" screen** (`ViewFieldCodes.html`, under **System Settings**) modeled on the Normalization manager: a table (field code · ORDERMATCH column as "V (22)" · source badge · actions) with an add form (column accepts "V" or "22"), inline edit, and a context-aware **Delete** (custom) / **Revert** (built-in) action. `data-layout="data"` + `app-measure` (matches the responsive frame), `fc*`-prefixed helpers (shared-scope safety), apostrophe-safe inline handlers (index, not value), themed via tokens. Wired into `App.html` (include + System-Settings nav item + `NAV_TITLES`/`navTo` keep-open/`markPendingViews`). Verified: `node --check`, 32 backend unit tests (fail-safe + override/add/invalid cases), `<div>` balance, no cross-fragment collisions.

### Fixed — Theme picker readable in the web app + blank views regression _(branch `experimental-themes`)_
- **Theme picker replaced with a custom dropdown** (`App.html`, `SharedUtils.html`). The native `<select>` option list is OS-rendered, and the Apps Script **web-app sandbox iframe doesn't propagate `color-scheme`** to it, so the picker rendered **light-on-light (unreadable) in dark themes in the web app** (it was fine in the modal, where the scheme is honored). The native `<select>` is now a `<button>` + a themed `<ul role="listbox">` — ordinary DOM fully under our CSS, so it renders with the theme's `--surface`/`--text`/accent tokens identically in the modal **and** the web app, in every theme. Opens upward in the vertical sidebar, downward in the Top Rail top bar; full keyboard support (Enter/Space/↓ to open, arrows/Home/End, Enter to select, Escape/outside-click to close), `aria-haspopup`/`aria-expanded`/`aria-selected`. `Theme.initPicker`/`syncPicker` drive it; the Classic wrapper (no picker) is guarded. Apostrophe-safe inline handlers (index, not value).
- **`color-scheme` declared per theme** (`SharedUtils.html`): base `:root` (Light, + Top Rail) = `light`; the dark blocks (`dark`/`top-rail-dark`, `dark-dense`, `midnight`, `cyberpunk`) = `dark`. Makes the **other** native controls (scrollbars, checkboxes, the dealer/map/user `<select>`s) render dark in dark themes, and fixes the modal's native popups deterministically. (Can't fix the web-app `<select>` popup — hence the custom dropdown above.)
- **Restored three blank views** (`ViewRules.html`). The wide-screen responsive pass accidentally turned a self-closed `<div id="rulesStatus" class="empty"></div>` into an unclosed `<div …>` when adding the `app-measure` class, leaving `#view-rules` open. The browser then nested the next three included fragments — **Normalization, Utilities, Pipedrive Settings** — *inside* the Rules view, so they inherited its `[hidden]` and rendered blank (modal and web app alike). Restored the closing tag; verified all ten views are `<div>`/`</div>`-balanced.

### Changed — Theming: binary light/dark → extensible theme registry _(branch `experimental-themes`; not yet deployed)_
- **The light/dark switcher generalized into an N-theme registry module** (`SharedUtils.html`, `App.html`, `Classic.html`, `Code.gs`). Themes stay **pure presentation** — CSS custom-property (`var(--…)`) overrides plus optional `data-theme`-scoped layout rules, all selected by the single `data-theme` attribute on `<html>` — so the `google.script.run` backend calls (which live in view JS and never read CSS) **cannot be broken by a theme**, including a structural one. `Theme` is now driven by a `themes` registry (`{id,label}`): `current()` falls back to Light for an unknown/empty id; `apply/set/initPicker/syncPicker` replace the binary `toggle()` (`cycle()` was dropped as unused). The sidebar light/dark toggle is replaced by a **native `<select>` picker wrapped in a `<label>`** (the whole row — including the glyph — focuses the control; glyph `aria-hidden`, select `aria-label`). The no-flash head bootstrap (App + Classic) now keeps **any** saved theme id, only following the OS when none is set.
- **Server theme persistence generalized** (`Code.gs`): `getThemePreference` / `saveThemePreference` validate via one `isThemeSlug_` (`/^[a-z][a-z0-9-]{0,31}$/`) instead of the hardcoded `'light'|'dark'`. Fail-safe: a well-formed but unregistered id is ignored on read → OS fallback, so rolling back the HTML alone never leaves a broken stored pref.

### Added — Experimental themes (Dark — Dense, Top Rail, Top Rail — Dark, Midnight HC)
- **"Dark — Dense"** — a dense, blue-accent dark theme; the copy-paste template for new themes (`SharedUtils.html` token block + `App.html` shell rules). Demonstrates the **structural** axis: the sidebar becomes a right-hand rail (`#appRoot` `row-reverse`, 188px, border flipped), with the active-nav accent moved to the rail's inner (right) edge so the selection cue keeps pointing at the content it selects.
- **"Top Rail"** — a structural theme (inherits the Light palette) that flips the side rail into a horizontal **top bar** (`:root[data-theme="top-rail"]` in `App.html`: `#appRoot{flex-direction:column}`, `#sidebar` row layout, active marker → bottom underline, brand divider swapped, footer hidden). The **System Settings** group — which can't stack inline in a bar — becomes a CSS-only hover flyout (`.nav-group-items` absolute, revealed on `:hover`/`:focus-within`, its `data-theme` selector out-specifying the base `.collapsed` rule so the JS toggle goes inert). _Known ceiling:_ the flyout is mouse-driven because the whole app nav is `<div onclick>` (mouse-only) in every theme — keyboard nav-activation is pre-existing app-wide debt, not introduced here; the `:focus-within` path is kept so it's keyboard-reachable for free once nav items get `tabindex`.
- **"Midnight HC"** — a token-only high-contrast near-black palette (`SharedUtils.html`): `--bg:#000`, near-black surface stack, white text, deliberately visible borders (`#5e5e5e`/`#6a6a6a`, ≥3:1 on the dark surfaces), a punchy `#ff5a2c` accent with `--on-accent:#000`, and a thicker focus ring. Logo lightens via the shared dark-logo selector.
- **"Top Rail — Dark"** — the Top Rail layout **composed** with the Dark palette, with **no duplicated rules**: the structural block's selector was widened to a prefix (`:root[data-theme^="top-rail"]`) so it serves both `top-rail` and `top-rail-dark`, and the new id was added to the Dark token block's selector so it inherits the Dark palette. Demonstrates structure × palette composition through the registry — adding the variant was one registry line + two selector widenings + a logo-selector entry.
- Adding a theme remains: push `{id,label}` to `Theme.themes` + add its `:root[data-theme="<id>"]` block(s); the picker and per-account persistence pick it up automatically. Gated by `test-verifier` (token resolution, no orphaned `data-theme`, no cross-fragment collision, flyout specificity) and a `ponytail-ux` review (Midnight contrast + the Top Rail flyout) before commit.

### Fixed — Top Rail scroll
- **Pages couldn't scroll in the Top Rail themes.** Flipping `#appRoot` to `flex-direction:column` made the main pane (`#appMain`) grow to full content height — `body{overflow:hidden}` then clipped the overflow instead of letting `#appContent` scroll. The base `#appMain` only sets `min-width:0` (what the default **row** layout needs); the column layout also needs `min-height:0`. Added `:root[data-theme^="top-rail"] #appMain{min-height:0}` (covers both Top Rail variants via the prefix), restoring the height chain `#appRoot`(100vh,column) → `#appMain`(flex:1,min-height:0) → `#appContent`(flex:1,min-height:0) → views scroll.

### Added — "Cyberpunk" super-experimental theme
- **A full neon-HUD reinvention** (token + shell + one webfont; `SharedUtils.html` + `App.html`, no Code.gs). A radical departure from the brand themes, still **pure presentation** (no markup/JS/backend change, fully rolls back). (1) **Token block** `:root[data-theme="cyberpunk"]`: deep blue-black surfaces (`#05070d`→`#1a2233`), **bright** body text (`#e6f7ff`) with neon reserved for accents/headings/borders/active (the cyberpunk readability trap, avoided), neon-cyan accent (`#00eaff` + dark `--on-accent`), neon semantics (`--danger:#ff3355` reddened so errors still read "stop", `--warning:#fde047`, `--success:#39ff14`, `--info:#b14dff`) + neon type pills with dark `--on-type`, sharp `--radius:2px`, cyan **glow** `--shadow`/`--focus-ring`, and **Rajdhani** HUD type. (2) **Shell block** (`App.html`): uppercase letter-spaced nav/brand/header, cyan `text-shadow`/`box-shadow` glow on brand/active/hover/header + neon-tube borders on `#sidebar`/`#appHeader`, and a faint cyan CRT **scanline** `#appContent::after` overlay (`pointer-events:none`). Views inherit the dark palette via their existing `#view-xxx{background:var(--bg)}` roots (so it's neon-on-dark throughout, like the other dark themes — the interim `.view{#fff}` guard is overridden by ID specificity). (3) Rajdhani added to the existing Google-Fonts `<link>` (additive — applies only when this theme sets `--font-*`). **Gated** by `test-verifier` (all static checks pass) + a `ponytail-ux` review: WCAG contrast verified (text ≥4.5:1, borders ≥3:1 on the dark surfaces — `--text-muted`/`--border`/`--border-2` were bumped to pass); review fixes applied = redder danger, nav tracking `.1em`→`.05em` (long sidebar labels), dropped the magenta aura + the unused Share Tech Mono font. (A review claim that views render white was checked against the fragments and **disproved** — all 9 carry themed `#view-xxx` roots.)

### Added — Web App entry point (full-screen browser, no modal)
- **`doGet(e)` web-app entry** (`Code.gs`) serves the **same** SilverFox App full-screen in a browser tab at the deployment's `/exec` URL — breaking free of the 1400×900 `showModalDialog`. It **reuses everything unchanged**: `App.html` + all views + every server function + `google.script.run` + the theme system (the shell is already `#appRoot{height:100vh}`, so it fills the viewport). **Additive** — the menu/modal (`openApp`) still works; this is a second door to the same app, fully reversible. (1) `doGet` mirrors `openApp`'s template setup (`initialTheme` + a new `appMode` flag) and returns the evaluated `App` with a title + responsive viewport meta. (2) `App.html` carries `data-mode="<?= appMode ?>"` on `<html>` (`modal` vs `webapp`); in `webapp` the **Close** item is hidden (`#navClose`) since a browser tab has no host dialog to close, and `exitApp` guards `google.script.host.close()` accordingly. (3) `appsscript.json` declares `webapp: { executeAs: "USER_ACCESSING", access: "DOMAIN" }` — each user runs as themselves, so per-user `getUserProperties` (theme, last-user) and the bound-spreadsheet `getActiveSpreadsheet` resolve per-person exactly as in the modal. **Deploy step is separate** (Apps Script *Deploy → Web app*, or `clasp deploy`) and is what mints the URL; first visit prompts each user to authorize the existing scopes (no new scopes added). _To verify live:_ the `getActiveSpreadsheet()` call sites (bound → SF_SYSTEM_MASTER) behave identically under the web-app context.

### Changed — Wide-screen responsive frame for full-screen web-app mode
- **The app now reads as a designed product at any width** (`SharedUtils.html` + `App.html` + all 10 view fragments). The SPA was tuned for the fixed 1400×900 modal and had **zero `@media` rules / no content max-width**, so at full-screen webapp width (1920/2560, or a resizable installed standalone window) content looked like "the modal, stretched" — fixed 440px form columns stayed small while the flexible side ballooned (a VIN textarea reached ~1440px), and number tables let columns auto-size and jump. **Pure CSS/markup; no backend, no logic, fully reversible. The locked 1400×900 modal is byte-identical** — every rule is gated behind `min-width:1500px` (the 1500 gate sits deliberately above 1400 so chrome rounding can't tip the modal into the wide rules). (1) **Mechanism:** new layout-measure tokens in base `:root` (`--measure-form` 880 / `--measure-form-wide` 1280 / `--measure-data` 1680 / `--gutter-wide` 48 / `--col-form-side: clamp(360px,32vw,440px)` — resolves to 440 at the modal). Each view root declares a `data-layout` tier and its content wrapper(s) carry an `.app-measure` marker; at ≥1500px the wrapper caps to its tier and `margin-inline:auto` centers it (extra space → calm gutters), `padding-inline` gutters at ≥2100px. The selector is `.view[data-layout] > .app-measure` (attribute only ever on a view root + marker on a direct child) so it can't leak in the shared CSS scope. **Width-gated, intentionally NOT `[data-mode]`-gated** — the installed standalone window benefits too; the modal is excluded by width alone. (2) **Tiers (Hybrid philosophy):** FORM views cap tight (Utilities 880; Run/Rules/Import/Pipedrive Settings/Data Sources/Transcription 1280); DATA views use the width but stay bounded (Home/VIN Logs/Normalization 1680). Flat multi-child views (Rules 4 rows, Norm 5 rows) tag each content row with the same measure so they stay column-aligned; Norm's `sizeNormTable()` height math is unaffected (it measures heights, not widths). (3) **Per-view internals:** Run's split column → `--col-form-side`, VIN textarea capped 1100px; VIN Logs runs table → `table-layout:fixed` + static 40/26/14/20% columns + ellipsis + `tabular-nums`; Home card grid capped `minmax(215px,260px)` + `tabular-nums` on the dashboard tables. Home's dashboard tables deliberately keep auto layout (dynamic per-section column counts — fixed columns would risk truncating real data; the `--measure-data` cap handles the stretch). _Verify:_ `clasp push`, confirm the modal first/last, then resize the `/exec` webapp narrow→1400→1920→2560 across all views under dark / top-rail / cyberpunk.

---

## [1.0] — GAS ShortCut OPS 1.0 (2026-06-25)

The system formerly versioned **SilverFox V2 (1.0–2.13)** is hereby released as **GAS ShortCut OPS 1.0** — the stable, deployed production baseline. `main` is the single deployed branch; everything below is live on it (deployed via `clasp push`). The version-history rows 1.0–2.13 that follow are retained as the historical record. Subsequent changes accumulate under `[Unreleased]` above.

### Changed — Dealer Rules + Transcription UX
- **Dealer Rules editor de-cluttered with tabs + collapsible advanced sections** (`ViewRules.html`). The two stacked full-width panels are now **top-level tabs** (`Filtering Rules | Pipedrive`) — only one shows at a time via the `[hidden]` attribute, each tab carries an "unsaved" dot wired into the existing `updateFilterDirty`/`updatePipedriveDirty` choke points, and switching keeps each panel's DOM + JS state so edits are never discarded (the app's hidden-views pattern). New `rules`-prefixed helpers `rulesTab()` / `rulesHelp()` (shared-global-scope safe per LEARNINGS). **Dual-Site Source Split** and the Pipedrive overrides are now native `<details>` **collapsed by default**, auto-expanding only when the loaded dealer actually has that config — derived from the model on every render so the state survives `pdDealerBody` rebuilds. The dense per-section hint paragraphs moved behind small **ⓘ info toggles** next to each section label. The former separate **"Deal-Field Overrides"** + **"Overrides (optional)"** blocks are **merged into one "Per Dealer Overrides"** disclosure (the per-group Active toggle stays outside it, auto-expand = any non-global field override OR any deal-default set). Tabs, help buttons, and disclosure summaries all gain `:focus-visible` rings (keyboard-accessible — closes part of the earlier UI-audit gap). **No `google.script.run` call, element id, `filtering_rules` JSON key, Pipedrive rows/productMap shape, or dirty-flag semantic changed** — pure IA/markup/CSS/JS-helper restructure of one view.
- **Transcription results now list newest-first** (`ViewTranscription.html`). Typed/pasted VINs render with the **most recently entered at the top**, so the latest Found / Not-Found result is always visible without scrolling a long list (previously appended at the bottom, hidden below the fold). Found/Not-Found counts are computed across every line as before; the 500-line render cap now keeps the **most recent** 500 ("Showing the most recent 500 of N").

### Added — Dealer Rules "Discard Changes"
- **"Discard Changes" on the Dealer Rules screen** (`ViewRules.html`): a per-panel button beside each Save button (Type Rules and Filtering Rules independently) that reverts that panel's unsaved edits back to the saved dealer config. `discardTypeRules()`/`discardFilterRules()` re-fetch `getDealerRulesData(currentDealerKey)` and re-apply ONLY their own panel (`typeRules`+`renderTypeRules()` / `applyFilteringRulesToUI()`), resetting just that panel's dirty flag — the other panel's edits are untouched. Each button is disabled unless its panel is dirty (wired via `updateTypeDirty`/`updateFilterDirty` + `disableSaveButtons`; `loadDealerRules` resets them on dealer switch), guards with `confirm()`, and re-enables on failure for retry. Reuses a tokens-only `.btn-secondary` style (themes correctly in light + dark).

### Added — Lot Sherpa visual redesign + theming
- **CSS design-token system + light/dark theming, end-to-end.** A Lot Sherpa brand restyle of the whole App. `SharedUtils.html` (include\_()-d first by both shells, so tokens reach every `#view-xxx` rule and the Classic fallback) defines the single source of truth: `:root` (Light) + `:root[data-theme="dark"]` (a neutral black→grey surface stack `#121214`→`#313137`, reserving the warm brand oranges — Coquelicot `#fd410d` accent, Coral hover, Rufous pressed — for accents only). Semantic, type-pill, type-scale, and space-scale tokens; **Poppins** (headings) + **Montserrat** (body) via Google Fonts. A new `Theme` helper (apply/toggle/current) persists the choice on UserProperties (`app_theme`) via new Code.gs `getThemePreference()` / `saveThemePreference()` (mirrors `getLastSelectedUser`); `openApp`/`openViewStandalone_` inject `initialTheme` into the template and a head FOUC script keeps the injected pref or follows the OS before first paint; `getAppBootstrap` returns `appTheme`. A sidebar sun/moon toggle calls `Theme.toggle()`. Default Light, follows OS on first run, remembers the user's choice.
- **All 9 view fragments migrated from ~506 hardcoded colors to tokens** (`ViewHome`, `ViewRun`, `ViewImport`, `ViewNorm`, `ViewRules`, `ViewVinLog`, `ViewTranscription`, `ViewUtilities`, `ViewDataSources`) — each gets a themed root (`#view-xxx { background:var(--bg); color:var(--text) }`); the Google-blue accent is replaced by Coquelicot orange throughout; Arial → Montserrat. Type pills repointed to `--type-new/po/cpo/cpoel`; Home dashboard banner/TOTALS-row inversion and VinLog SPLIT badge re-tokenized. (An interim `.view` light guard added during migration is now superseded by the per-view roots.)
- **SilverFox fox-emoji replaced by the real logo** — the supplied logo (Asset 68) rendered as an alpha silhouette and embedded as a base64 CSS `mask-image` on the sidebar brand mark, so `background-color` themes it cleanly (neutral dark-grey in light mode, light silver in dark mode; eye sockets read as negative space). The "SilverFox" wordmark text stays beside it.
- **Dealer Rules screen redesign** (`ViewRules.html`): the cramped two-column Type | Filtering split replaced with **stacked full-width panels** (whole view scrolls as one column; sticky section title bars; each panel keeps its own Save). The **Exclude-from-CAO pills moved directly under Allowed Types** (both are type pickers). The **Add Rule form redesigned** to read as a sentence ("When type is → Use CSV schema / UTM medium", orange arrow connector; fixed-width selects so it can't overflow its panel). Filtering sections laid out as a **responsive card grid** — Flags / Allowed Types / Exclude from CAO / Exclude Status as four equal-height columns (row 1), Price Range + Seasoning side-by-side and height-matched (row 2), with Targeting Rules and Dual-Site Source Split as full-width cards below.
- **Two pre-existing layout bugs fixed in passing:** Home dashboard bottom rows were clipped/unscrollable (`.home-scroll` missing `box-sizing:border-box`); a long UTM-medium string pushed a Type Rules card past its panel and shoved the Remove button out of reach (`min-width:0` on the rule field/grid so it truncates with an ellipsis).

### Fixed — import health
- **Health baseline read is now deterministic.** Added `SpreadsheetApp.flush()` between `writeImportStats_` (which appends the current import's IMPORT_STATS rows) and `checkImportHealth_` (which immediately reads history back via `getLastRow()` + a tail window). Without the flush a stale `getLastRow()` could shift the `HEALTH_TAIL_ROWS` window and make the baseline non-deterministic. The flush sits outside both functions' try/catch so their non-fatal isolation is preserved. (Hardening surfaced while investigating a "health on merge" report — not the fix for that symptom; the merge path already computes review + health on the final merged dataset.)
- **The "Location missing from this import entirely" check now compares against only the most-recent prior import, not the whole 2,000-row history tail.** When the scraper feed renames a location (e.g. "BMW of West St Louis" → "BMW of West St. Louis"), the old name lingers in the history tail and was exact-string-flagged "missing" on *every* import forever (current data only ever carries the new name, so more data could never clear it). Now `checkImportHealth_` builds a `{timestamp: {location: true}}` map of locations that carried real inventory (`total > 0`) per prior import, finds the latest timestamp `< currentTs`, and flags a location "missing" only if it was present in *that* import but is absent now. A renamed/retired feed name ages out after one import; a genuine same-name disappearance is still caught. Only the missing-location block changed (drop/zero/missing-field checks, the non-fatal try/catch, and the 13-col read untouched); unit-tested both directions.

### Fixed — UI
- **Run Order screen made readable in dark mode** (`ViewRun.html`). Two patterns the tokenization pass had missed: (1) form controls (Running-as / Dealer selects, Pipedrive + Second Deal ID inputs, the VIN textarea, finalization-card Deal ID inputs) set a border but no color/background, so they fell back to the UA near-black default that ignores `data-theme` — dark text on a dark control; added `background: var(--bg); color: var(--text)`. (2) colored-fill buttons used `color: var(--bg)`/white → dark text on the orange fill in dark mode; remapped Finalize / Run Dealer → `var(--on-accent)` and Add to VIN Log → `var(--on-success)`.

### Config (live sheet edit — not a git commit)
- **BMW of West St. Louis `scraper_location_name` corrected** in `SF_DEALER_CONFIG → DEALERS` col J (cell J6). The feed renamed the location to "BMW of West St. Louis" (with a period) but col J was "BMW of West St Louis" (no period); the exact-match miss made `getDealerScraperData_` pull zero inventory for BMW (CAO/runs broken) and triggered the false import-health warning the fix above ages out. Set J6 to "BMW of West St. Louis". A full audit of all 29 active dealers (config-rules-reviewer subagent) found BMW was the **only** definite drift — Serra Honda O'Fallon and CDJR of Columbia ("Joe Machens Chrysler Dodge Jeep Ram") config values DO match their feed names; 12 dealers were unconfirmed only because they weren't in the latest import.

### Chore
- **Project subagents added** under `.claude/agents/` — `frontend-specialist`, `backend-specialist`, `config-rules-reviewer`, `test-verifier`, `docs-curator` (+ a test-verifier note on Windows `fs.readdirSync` vs `ls`). Dev tooling only; not part of the clasp deploy.

### Added — `feature/frank-leta-dual-site` (merged to main June 2026)
- **Dual-site dealer support (Frank Leta main site + AutoLoanPro) — one order, two CSVs.** New optional **`source_split`** key in `filtering_rules` (col W): `{ "group_name": "AUTOLOANPRO", "url_contains": "autoloanpro" }`. A single Frank Leta run produces **one billing sheet and one Pipedrive deal**, but the matched cars are split into **two CSV outputs** — `CSV` (main site) + `CSV_AUTOLOANPRO` (subprime) — by whether the Vehicle URL contains the marker (`buildCSVSheet_` gains a `sourceSplit` param; QR/UTM/RUN_LOG unchanged). The single BILLING sheet gains a **"BY SOURCE (QTY PER SKU)"** section breaking the matched count down per source × type (`Main Site — PO`, `AutoLoanPro — PO`, … + per-source totals) so the correct quantity per SKU can be entered in the one Pipedrive deal. Labels are source-prefixed so they never collide with `readBillingTotals_`'s RUN_LOG type totals. **Configurable in the UI:** the Dealer Rules editor's Filtering tab gains a "Dual-Site Source Split" section (enable toggle + group name + URL-contains) that reads/writes the `source_split` key (`ViewRules.html` only — `source_split` added to `FILTER_MANAGED_KEYS`; emitted only when enabled and both fields filled; no server change). The "check main first, then AutoLoanPro" waterfall is resolved at **import**: `dedupeScraperRows_` now auto-keeps the **main-site** listing when the same VIN appears with a main URL and a marker URL **within a configured dealer's Location** (`getSourceSplit_` / `getSourceSplitLocations_`) — automatic, order-independent (works no matter which feed is imported first or in separate merges), and **scoped by Location so no other dealer's import is affected**. Fail-safe: with no `source_split` configured (or no marker URLs in the data) behavior is byte-identical to today. Activation requires adding `source_split` to Frank Leta's col W. (Built after the Data Sources feature, per plan; deferred originally for it.)

### Added — `feature/data-sources` (deployed + merged to `main` June 17, 2026 — tag `stable-post-targeting-rules`)
- **Configurable data sources — header mapping + new columns.** New **Data Sources** sidebar screen (`ViewDataSources.html`): pick a dealer + a **named source** (import format) + a sample CSV, map each file header to a canonical column (pre-filled from the saved mapping + exact-name match; VIN required), and **Save Mapping**. The normal **Import** screen resolves saved headers as aliases (global union — the scraper's canonical headers are unaffected), so a feed with different labels/order lands in the right columns automatically.
  - **Multiple sources per dealer (no clobbering):** `SOURCE_MAPPINGS` is keyed per `(dealer, source_name)` — a dealer can have several import formats (e.g. its scraper feed *and* an internal-system CSV), each mapped independently; saving/deleting one never touches another. Server: `getSourcesForDealer`, `getSourceMapping(dealer, source)`, `saveSourceMapping(dealer, source, json)`, `deleteSource`; `getHeaderAliasMap` union unchanged.
  - **Declare brand-new columns (schema growth):** a **"＋ Add new column…"** dropdown entry calls `addSchemaColumn(label)` — appends a row to a `SCHEMA` tab (seeded with today's 21) and widens SCRAPERDATA by one column (**append-only**, so every fixed index ≤ col U is untouched). New columns are **store-only** (captured/deduped/written by the import, but not in the ORDERMATCH QUERY / CSV / filters / stats until separately wired). Column count is now driven by `getSchemaColCount_()` (cached `getDataSchema_`) at every master-import site; the run path stays at the base 21. **Prerequisite:** the scraper timestamp is relocated out of `SCRAPERDATA!W1:X1` into a dedicated **`META`** tab (so columns can grow past col V without colliding) — `fillScraperDateTime`/`computeImportToken_`/`getAppHomeStatus`/`getTranscriptionVins`/`onEdit` all updated via `getTimestampMeta_` (META-first, W1:X1 fallback pre-migration).
  - **Deploy gating:** the mapping half is additive/fail-safe; the schema-growth half mutates the live SCRAPERDATA schema + the timestamp, so it ships in a maintenance window with Version-history backups and the recorded original `W1:X1`. Fully backward-compatible until a column is actually added.
  - **Testing-round follow-ups:** (a) Data Sources screen now **shows/edits a saved mapping without re-uploading a file** (`getSourceMapping` returns the ordered original-case headers) and the source picker exposes view/delete; (b) **targeting-condition Field dropdown reads the whole data schema** — `FILTER_FIELD_INDEX` replaced by a cached, schema-driven `getFilterFieldIndex_()` so conditions can target **any** column incl. newly-added ones (`getDealerScraperData_` now reads full width for filtering; `pasteScraperData_` slices back to the base 21 for the output doc); (c) new **Require URL** flag (reject vehicles with no Vehicle URL, reason `no_url`); (d) new **`drop_on_import`** condition operator + a per-dealer, Location-scoped import drop pass (`getImportDropLocations_`/`dropRowsOnImport_`, runs before dedup) — used to **drop subprime cars from a dealer's direct feed on import** (so they never pollute the dealer's data or flag conflicts); the import review reports the dropped count.

#### Changed — Targeting Rules: unified IF (nested AND/OR) THEN action engine (replaces `conditions`)
- **The "Targeting Conditions" table in the Dealer Rules editor is replaced by a nested rule builder.** Each rule is **IF** (conditions combined with nested **AND/OR** groups) **THEN** an *action*: **Drop on import**, **Exclude from CAO**, or **Exclude from orders**. This folds the shoehorned `drop_on_import` **operator** into a first-class **action** and lets a single rule target on multiple conditions at once (`(A AND B) OR C`). **Only that section changed** — Allowed Types, Exclude Status, Require flags, Price Range, Seasoning, the **Exclude-from-CAO pills** (`cao_exclude_types`), and Dual-Site Source Split are all untouched (the pills stay as the simple, ease-of-use path; the rule builder is the advanced path).
  - **Data model:** new **`targeting_rules`** key in `filtering_rules` (col W) — an array of `{ action, group }` where a `group` is `{ match: "all"|"any", children: [ {field, op, values} | nested group ] }`. Replaces the flat `conditions` array and its `applies_to` (now expressed as a `{type, in, […]}` condition AND'd into the group).
  - **Engine (Code.gs):** `evaluateCondition_` refactored into **`conditionMatches_`** (raw predicate) + **`groupMatches_`** (recursive AND/OR) + **`ruleMatches_`**. Polarity flips from *inclusion* ("keep if it matches") to *exclusion-on-match* ("if it matches, act"), so the predicate now **fails SAFE** (unknown field/op, empty values, unparseable number, empty group → **no match → no exclusion**), the opposite of the old fail-open — a config typo can no longer mass-exclude a dealer's inventory. New ops **`gt`/`lt`** added (the migration's `gte`→`lt` / `lte`→`gt` inversions need them); the `drop_on_import` **op** removed from `FILTER_OPS` (now `TARGETING_ACTIONS`). `applyFilteringRules_` applies `exclude_order` rules in both CAO + run phases and `exclude_cao` rules in CAO only (Bypass still overrides per run); `getImportDropLocations_`/`dropRowsOnImport_` now evaluate `drop_on_import` **action** rules through the same nested engine. `getDealerFilterRules_` returns `targetingRules` (drops `conditions`, keeps `caoExcludeTypes`); `getRulesEditorBootstrap` returns `filterActions` + `gt`/`lt` ops.
  - **UI (`ViewRules.html`):** recursive builder — client `targetingRulesModel` is the source of truth (mutate-on-edit, re-render on structural ops, Values input updates in place to preserve focus); per-rule Action dropdown, per-group ALL/ANY toggle, `+ Condition` / `+ Group`, nested removal. `FILTER_MANAGED_KEYS` swaps `conditions`→`targeting_rules` (and now lists `require_url`); a not-yet-migrated dealer's legacy `conditions` key is dropped on load so it can't round-trip as an inert passthrough.
  - **Migration (done June 17, 2026):** the **4 dealers** with a `conditions` key were rewritten — **Pundmann Ford, Bommarito Cadillac, Dave Sinclair St. Peters, Frank Leta Honda** — preserving exact behavior (incl. missing-price/year → still kept). Mazda of Columbia (only `cao_exclude_types`) untouched. **Verified live** after `clasp push`: full-sheet check confirmed 0 stale `conditions` keys and `drop_on_import` only as Frank Leta's action; 25 behavioral-equivalence assertions against the deployed engine; UI round-trip in the Dealer Rules editor. Exact before/after JSON + rollback steps: **`docs/targeting_rules_migration.md`**.

### Changed
- **Run Order: stackable VIN fills + automatic submit de-dup.** "Pre-fill from CAO" now **appends** instead of replacing, and a new **🔍 Fill from Transcription** button (next to it) appends the VINs entered in the Transcription view — so CAO and manual/transcribed lists can be combined into one order (press both; neither clears the box). On submit, the order VIN list is **de-duplicated** (case-insensitive, order-preserving) both client-side (with a toast noting how many duplicates were dropped) and server-side in `pasteVinsAndRun` (guarantees it regardless of entry path), so a VIN can never be ordered twice. Transcription tab moved up the sidebar to sit just under Run Order.

### Added
- **In-app Transcription tab — live "Found / Not Found" VIN check.** New **🔍 Transcription** sidebar view (`ViewTranscription.html`) mirrors the TRANSCRIPTION sheet's ARRAYFORMULA without leaving the app: paste/type VINs (one per line) and each is checked **instantly client-side** against the current inventory. New `getTranscriptionVins()` returns the deduped, normalized set of SCRAPERDATA col-A VINs (+ last-import timestamp); the view loads it once on open into a `{VIN:1}` index and re-checks on every keystroke (no per-keystroke round trip), showing a running Found/Not-Found tally and a per-line badge list. A **Refresh** button re-pulls the set after a new import.
- **Live Dashboard on the App Home page + Utilities moved to its own sidebar page.** Home now renders the DASHBOARD sheet (Inventory Snapshot + TOTALS, Run Log Summary, Most Recent Run, Runs By Dealer) below the workflow cards, via a new `getDashboardView()` server function (returns the tab's display values) and a client renderer that stacks each section as a styled table. It **reloads on every visit to Home** (`VIEW_SHOWN`) and has a manual **Refresh** button, so the user can check current numbers between order runs without closing the app — the run sections are formula-driven over RUN_LOG, so they reflect freshly finalized runs. The five **Utilities** buttons moved off Home into a new **`ViewUtilities.html`** sidebar page (🔧 Utilities); the Home status strip is now derived from the dashboard's "Last Import" row (one round-trip instead of a separate `getAppHomeStatus` call). App shell updated: include, sidebar item, `NAV_TITLES`.

### Fixed
- **Import conflict cards were hiding the "New" column.** The VIN-conflict resolution panel showed only the *Existing* side of each conflict — the *New* column was clipped out of view, so users couldn't compare the two occurrences. Cause was a CSS regression from the 1400×900 layout rework (`0c7f126`): `#conflictList` became a 2-column grid, halving each card's width, and the `overflow:hidden` card + default `table-layout:auto` table (with a fixed 130px Field column) pushed the third column past the clipped edge. Fix: `#conflictList` back to a single full-width column, and `table-layout: fixed` on `.conflict-table` so Field/Existing/New always get allocated width and long values wrap instead of clipping. Render logic, dedupe engine, and conflict count are unchanged.

### Performance — `feature/app-shell` efficiency sweep
- **One bootstrap call per App session** — new `getAppBootstrap()` returns dealers + user profiles in a single execution; the App prefetches it while the user is on Home (shared `AppData` latch in SharedUtils), so Run Order and VIN Logs first visits populate instantly instead of firing three separate cold-start executions. Merge-mode caption now notes that Merge never removes sold vehicles (run a Main Import periodically).
- **Import path trims** — DASHBOARD data-row formatting batched from ~500 per-row range ops to 7 block ops per refresh (`setBackgroundObjects` matrix for the stripes); column widths/frozen rows only set on first layout; `groupRowsByLocation_` switched from O(n²) `concat` re-copying to `push.apply` per bucket; `checkImportHealth_` reads only the last 2,000 IMPORT_STATS rows (rolling baselines need far less — keeps the read constant-time as the tab grows).
- **Early-exit recalc waits** — the fixed post-formula sleeps (ORDERMATCH up to 3.5s, LINKBUILDER up to 2s) are now 250ms polls via new `waitForRecalc_` that exit as soon as the QUERY spill / last link formula materializes; worst case unchanged. Saves ~1–4s per run.
- **Parallel QR uploads** — `generateQRCodesParallel_` now uploads all PNGs via parallel multipart requests to the Drive REST API (`UrlFetchApp.fetchAll`, 50/batch, per-file DriveApp fallback on errors) instead of one sequential `createFile` per PNG. Expected 6–24s → ~1–3s per run.
- **QR folder hygiene + batch Drive operations (the "minutes/hangs" fix).** Dealer QR folders accumulated every run's PNGs forever (duplicate filenames included); `abandonRun` then iterated the entire folder with one sequential `setTrashed()` per file (~120ms each — 60+ seconds at a few hundred files). Now: (1) the dealer's QR folder is **auto-cleared at run start** (old PNGs → Drive trash, 30-day recovery) so folders only ever hold the current run; (2) new `trashFilesParallel_` batch-trashes via the Drive REST API with `UrlFetchApp.fetchAll` (100/batch) — `clearQRFolder_` and abandons complete in ~1–2s regardless of file count; (3) `generateQRCodesParallel_` returns the created **file IDs**, carried on `pendingRuns` entries, so `abandonRun(dealerKey, outputDocId, qrFileIds)` trashes exactly this run's files by ID (legacy name-scan kept as fallback).
- **Spreadsheet-handle caches**: `getMasterSS_()` / `getVinLogsSS_()` mirror the existing `getConfigSS_()` single-open-per-execution pattern; all 14 scattered `openById(MASTER_SHEET_ID/VIN_LOGS_ID)` call sites routed through them (saves ~0.4–1s on executions touching these docs more than once; `getActiveSpreadsheet()` sites intentionally untouched per LEARNINGS).

### Added — `feature/app-shell` (the SilverFox App SPA — merged to `main`)
- **Run Order view migrated (commit 7) — all five views now live in the App.** `DealerSelector.html` → `ViewRun.html` fragment (incl. the new split-deal + post-run finalization flow): scoped CSS, run-prefixed ids/helpers, lazy `initRunView`. **Cross-view jump:** "Update VIN Log" now switches to the VIN Logs view with the dealer preselected — pending finalization cards **survive** the jump (hidden views keep their state; the old dialog swap destroyed it). Run-side `AppBusy`: a run blocks imports and vice-versa; close guards cover live runs and un-finalized cards; Cancel/Close routes through the shared guarded `exitApp()`. Classic "Run Dealer..." serves the same fragment.
- **Dealer Rules view migrated (commit 6)** — `RulesEditor.html` → `ViewRules.html` fragment: CSS scoped under `#view-rules` (root gets `position:relative` so the loading overlay covers only this view), `status`/`dealerSelect` → `rulesStatus`/`rulesDealerSelect`, `setStatus`→`setRulesStatus`, shared `escHtml`, lazy `initRulesView` (bootstrap), and a close guard for unsaved `typeDirty`/`filterDirty` edits. Classic "Edit Dealer Rules..." serves the same fragment.
- **VIN Logs view migrated (commit 5)** — `VINLogUpdater.html` → `ViewVinLog.html` fragment: CSS scoped under `#view-vinlog`, `status`/`dealerSelect`/`vinCount` → `vinlogStatus`/`vinlogDealerSelect`/`vinlogVinCount`, `setStatus`/`updateVinCount` → `setVinlogStatus`/`updateVinlogVinCount`, lazy `initVinlogView` (dealer load + listeners), Close → guarded `exitApp()`. New `vinlogPreselectDealer(key)` for the upcoming Run→VIN Logs cross-view jump (stashes the key if dealers are still loading). Classic "Update VIN Log..." serves the same fragment.
- **Import view migrated (commit 4)** — `ScraperImport.html` → `ViewImport.html` fragment: CSS scoped under `#view-import`, `status`→`importStatus`, `setStatus`→`setImportStatus`, local `escHtml` removed (SharedUtils copy), panel heights `100vh`→`100%`. New **AppBusy interlock**: importing is blocked while a dealer run is active and vice-versa (an import rewrites SCRAPERDATA mid-run — impossible when modals were exclusive); the import stays "busy" through conflict resolution until Apply/Cancel. Close guard registered (in-flight/conflict-pending import warns on exit). SharedUtils include moved to the TOP of both shells (fragments register guards at parse time). Classic "Import Scraper Data..." serves the same fragment.
- **Normalization view migrated (commit 3)** — `NormManager.html` → `ViewNorm.html` fragment (pattern-setter for the remaining views): CSS scoped under `#view-norm`, `status`→`normStatus`, `setStatus`→`setNormStatus`, `sizeTable`→`sizeNormTable` rewritten to measure the view container with view-scoped queries (window-based sizing is wrong inside the shell; global `.top-bar` queries would hit other views), `addEventListener('resize')` instead of claiming `window.onresize`, lazy `initNormView` via the view registries, Close → guarded `exitApp()`. Classic "Manage Normalization Maps..." now serves the same fragment via `openViewStandalone_`. Sidebar item enabled.
- **Home utilities live (commit 2)** — the five utility actions run from the Home page with in-app toast feedback and double-click protection; destructive ones (Clear QR Folders, Clean Up Old Output Docs) confirm first. Core/wrapper split in Code.gs because `ui.alert()` fails when invoked via `google.script.run`: `eraseAllQRFoldersCore_`/`appEraseAllQRFolders`, `cleanUpOutputDocsCore_`/`appCleanUpOutputDocs`, `refreshNormReferenceCore_`/`appRefreshNormReference` (menu wrappers keep their alerts); `fillScraperDateTime` returns `{message}` (refreshes the Home status strip); new `appOpenRunLog` activates the RUN_LOG tab behind the modal.
- **SilverFox App shell (commit 1)** — single-modal SPA foundation: new `App.html` (left sidebar nav + header + content area, Gmail-style), `ViewHome.html` (workflow launcher cards + utilities placeholder + last-import status strip via new `getAppHomeStatus()`), `SharedUtils.html` (shared `escHtml`, `toast`, `AppGuards`, `AppBusy`, view registries), `Classic.html` (standalone single-view wrapper for the fallback menu). Code.gs: `include_()` template helper (first HtmlService templating use), `openApp()`, `openViewStandalone_()`. Menu rewritten: **🚀 Open SilverFox** + "Classic menu (deprecated)" submenu with all 10 existing items (fallback during validation). The five modal views migrate into the App one commit at a time; un-migrated sidebar items are dimmed "coming soon".

### Known issues
- `CDJR_OF_COLUMBIA` `scraper_location_name` intentionally remains `"Joe Machens Chrysler Dodge Jeep Ram"` to match the live scraper feed; update when the feed reflects the new dealer name
- Dave Sinclair Lincoln: used cars have no price in the scraper feed, so a "used ≥ $35k" targeting rule cannot function until used prices are scraped

### Planned
- **Log capacity plan (documented; build at trigger)** — "Capacity & Log Growth Plan" section added to the Bridge doc: hard limit 10M cells/spreadsheet, practical limit (full-tab readers: `checkImportHealth_`, DASHBOARD QUERY, `getRunsForDealer`) at ~25k–50k rows ≈ 2–3 years at production pace. Trigger: any log tab > ~25k rows or visible slowdown → build menu-driven `archiveOldLogs()` (rows older than 12 months → `SF_LOG_ARCHIVE`, per-year tabs; no live function references the archive; never archive SF_VIN_LOGS).
- **Trim cleanup (analyzed; deferred)** — docs-only for now: full analysis + a validated auto-cleanup design (global `cleanTrim_` regex pass behind an `ENABLE_TRIM_CLEANUP` flag + `dryRunCleanTrim_` preview, plus residual exact-match rules) written into the Bridge doc ("Trim Normalization & Cleanup — Analysis & Deferred Design"). Approach decision (A full / B phased / C exact-only) pending.
- Unresolved order configurations: Auffenberg Hybrid (Courtesy Loaners NEW→USED)
- Trim cleanup approach decision (A full / B phased / C exact-only) — see Bridge doc deferred-design section
- Architecture hardening: IFERROR-wrapped ORDERMATCH formulas, self-describing field-to-column map, resumable runs (6-minute Apps Script ceiling), regression harness, scheduled config audits, extended per-run caching

---

## [2.13] - 2026-06-25 — Dynamic vehicle-type registry

Merged to `main` (the single deployed branch) on 2026-06-25 (`feature/dynamic-vehicle-types`). Backend Code.gs + UI `ViewRules.html` / `ViewImport.html`. The next minor after v2.12 Pipedrive. **Deployed and active** — after `clasp push` the user added a real custom type and ran an end-to-end test order that worked; the live `PIPEDRIVE_SETTINGS` now carries a `vehicle_types` row. With no extra types added the registry falls back to exactly the canonical four, so behavior is byte-identical to before (the deploy was safe to ship dark).

### Added
- **Vehicle type is now a dynamic registry instead of a hardcoded four.** `New` / `PO` / `CPO` / `CPO-EL` were baked into ~14 sites; they're now the protected built-ins of a registry that the user can extend on the fly from the Dealer Rules screen, and a new type is recognized everywhere — Allowed-Types / CAO pills, seasoning, the Pipedrive product picker, billing, line items, and analytics.
  - **Single source of truth + a guaranteed floor.** New constant `CANONICAL_TYPES = ['New','PO','CPO','CPO-EL']`; the extras live as a JSON array in the **`PIPEDRIVE_SETTINGS`** tab (SF_DEALER_CONFIG) under key **`vehicle_types`** (`PD_VEHICLE_TYPES_KEY`). **`getCanonicalVehicleTypes_()`** returns the de-duplicated (case-insensitive) union of `CANONICAL_TYPES` (always first, in order) + the stored extras, cached per execution. **Fail-safe:** the built-ins are always present even if the stored value is missing/partial/malformed — a stored value can only ever *add*, never drop or reorder the protected defaults. `PD_TYPE_KEYS` was **removed**, folded into `CANONICAL_TYPES` + a new **`CANONICAL_BILLING_FIELDS`** (canonical type → legacy billing-field names).
  - **Client-callable registry API.** `addVehicleType(label)` validates (non-blank, ≤40 chars, no case-insensitive duplicate, and rejects reserved billing-sheet labels — "Total Ordered" / "Total Duplicates" / etc. and any "… Dupes" name that would confuse `readBillingTotals_`'s row parsing) and returns the updated list; `removeVehicleType(label)` is guarded (see remove guard below) and returns `{ok, types?, blockedBy?, message?}`; `getVehicleTypes()` reads the list. Surfaced to the editors via `getRulesEditorBootstrap` (`vehicleTypes`) + `getPipedriveDealerEditorData` (`types`). Helpers `getExtraVehicleTypes_` / `isCanonicalType_`.
- **Backend billing/run go dynamic — RUN_LOG / ORDER_STATS stay byte-identical** (Code.gs):
  - **`readBillingTotals_`** now returns a **`byType: {<type>:{gross,dupes}}`** map (registry-driven row labels) **and keeps the legacy `totalNew`/…/`cpoElDupes` fields**, derived from `byType` via `CANONICAL_BILLING_FIELDS` — so RUN_LOG cols G–N and ORDER_STATS are unchanged (`writeRunLog_` untouched there). `byType` is the dynamic interface; the canonical fields are the back-compat shim.
  - **`buildLineItems_`** iterates the mapped product-map types reading `billing.byType[type].gross` (was `PD_TYPE_KEYS` + `billing.totalNew`); **`bySourceToBilling_`** now emits the `byType` shape. Quantity stays **GROSS** (a VIN-log dupe is still produced and billed).
  - **`renderBillingSheet_`** `TYPE_ORDER` is now `getCanonicalVehicleTypes_()` — a registered type gets a clean fixed row; "⚠ unexpected type" now means "in the data but not registered."
  - **`buildTypeRulesFromProductMap_`** replaced its hardcoded `['CPO-EL','CPO','New','PO']` order with **sort-by-length-descending (longest match first)** — the generic substring-safety rule for `matchRule_` (which is `indexOf`-based, so `CPO` must not precede `CPO-EL`); yields the same order for the canonical four and orders any user-added type safely.
- **UI — Dealer Rules (`ViewRules.html`).** Allowed-Types + CAO pills and the seasoning dropdown render from the registry (`renderTypePills`, preserving selection across re-renders). A **"+ Add type"** control (`addNewType` → `addVehicleType`, auto-selects the new type for the current dealer); a **× on user-added pills** (`removeTypeFromPill` → `removeVehicleType`, guarded) — built-ins (`BUILTIN_TYPES`) never get a ×. The type is read from the pill's `data-type`, **never interpolated into the inline handler** (apostrophe-safe, per the existing inline-handler LEARNING). The **Pipedrive product picker is filtered to the dealer's Allowed Types** via `pdEffectiveTypes()` (none selected = "all types pass" → show all), re-rendered when Allowed Types changes (toggle/add/remove) and after the filtering panel loads (handles the dealer-change load-order race). **A hidden type's product mapping is preserved automatically** because `buildProductMap_` serializes the MODEL (`pdSelections[group].productMap`), not the rendered rows — filtering only changes what `pdRenderProductRows_` renders.
- **Assignment is a separate, deliberate step (register-only by decision).** "Add New Type" only *registers* the label; a new type stays **inert until vehicles normalize to it** — the user maps a raw scraper value to it in **Manage Normalization** (or the feed already uses the label). The Add UI shows a hint saying so.
- **Remove guard scans EVERY type-reference site.** `removeVehicleType` refuses a built-in (`isCanonicalType_`) or an **in-use** type; **`dealersUsingType_`** scans every dealer's `filtering_rules` (col W) + every PIPEDRIVE row and returns the blocking dealer names so the UI can explain. `filterRulesUseType_` checks **allowed_types, cao_exclude_types, seasoning[].type, billing_split (field:"type"), and targeting_rules conditions (`{field:"type"}`, recursing nested AND/OR groups)**; `pdRowUsesType_` checks `product_map` + `source_product_map` keys. *(The `ed0b5f8` config review caught that `targeting_rules` + `billing_split` were initially missed — live dealers Bommarito / Pundmann / Dave Sinclair St. Peters use `{field:"type"}` targeting conditions, so an in-use type could have been wrongly removed leaving a dangling condition — and that gap was fixed.)*
- **Per-type analytics — dynamic dashboard + a new long-format tab (fixed log schemas untouched).** Decision: per-type history goes in a long-format tab + a dynamic dashboard; **RUN_LOG / ORDER_STATS / IMPORT_STATS keep their fixed canonical-four columns** (a new type folds into existing totals / "Other" there).
  - **New `ORDER_TYPE_STATS` tab** (SF_SYSTEM_MASTER): `timestamp | dealer_key | dealer_name | order_id | type | produced | dupes` — one row per type present in a run (gross or dupes > 0), written by `writeRunLog_` from `billing.byType` in its own non-fatal try/catch (auto-created via `createOrderTypeStatsSheet_`). Complete per-type history for **any** type with no schema change; ports 1:1 to a V3 Postgres table.
  - **`computeImportReview_`** tallies inventory per registered type into `locationDetail.byType` (the canonical `new/po/cpo/cpo_el/other_types` fields are kept for the fixed IMPORT_STATS).
  - **`refreshDashboard_`**: the INVENTORY snapshot gets a **dynamic column per registered type + "Other"** (unregistered types), and a new **RUNS BY TYPE** section (per-type COUNTIF/SUMIF over `ORDER_TYPE_STATS`; the criterion references the type cell so a quoted label can't break the formula). RUNS BY DEALER stays the last, spilling QUERY (its per-type sums still read the fixed RUN_LOG cols G–J). Clear range widened (rows + cols) for the added block.
  - **ViewImport** ("unexpected type" review badge) now reads the registry (`getVehicleTypes` → `window.importKnownTypes`) so a registered type isn't flagged (cosmetic — badge color only).
- **Template formulas updated LIVE (behavior-preserving) for custom types.** The printed field codes `TYPESTOCK` (ORDERMATCH N2) and `TYPEVIN` (R2) are ARRAYFORMULAs in SF_UNIVERSAL_TEMPLATE, not script-written; their old `IF(SEARCH("CPO-EL"…),…,"USED - ")` cascade hardcoded the four and defaulted **every** other type to "USED", so a custom type printed as USED. Reworked to an **exact type-match cascade** (`UPPER(TRIM(G))="CPO-EL"` / `="CPO"` / `="NEW"` / `="PO"→USED`) with an `UPPER(TRIM(G))&" - "` else, so a custom type prints **its own uppercased name** (canonical `New`/`PO`/`CPO`/`CPO-EL` unchanged — behavior-identical for the four). Also removes a latent substring bug (a custom name containing `po`/`cpo`/`new` could misclassify). Applied + verified live via the Sheets MCP (a "Loaner" type → "LOANER - "); no code or clasp change.

### Removed
- **`PD_TYPE_KEYS`** — the old `[{type, gross, dupes}]` array, folded into `CANONICAL_TYPES` + `CANONICAL_BILLING_FIELDS` (see Added).

### Verification
- `test-verifier` all gates pass (syntax, `PD_TYPE_KEYS` removed, no SPA cross-fragment collisions, 31/31 unit tests, byType + legacy back-compat); `config-rules-reviewer` confirmed behavior-preserved and caught/fixed the remove-guard gap above. **Verified live after `clasp push`** — a real custom type was added and an end-to-end test order ran successfully.

---

## [2.12] - 2026-06-24 — Pipedrive Integration

Merged to `main` (the single deployed branch) on 2026-06-24 — the full Pipedrive arc (`pipedrive-integration` + its stacked sub-branches `feature/pipedrive-finalize-flow`, `feature/pipedrive-install-cost`, `feature/pipedrive-followups`, `feature/pipedrive-billing-pdf`, and `feature/product-driven-schema`). All of it lives in Code.gs **Section 31** plus `ViewRules.html`, `ViewRun.html`, `ViewVinLog.html`, and the new global `ViewPipedriveSettings.html`. Realizes the long-standing Pipedrive to-do. **Live config required to activate** (the feature is inert until it's filled in): ScriptProperties secrets, the `PIPEDRIVE_SETTINGS` global rules, and the per-dealer `PIPEDRIVE` rows (incl. the product map — see "product map is now the sole per-type config" below).

### Added
- **Pipedrive API integration — push a finalized run to Pipedrive as a deal with per-type product line items.** The push is a **separate, explicit step taken AFTER in-system finalization** — never automatic, and never before the run's RUN_LOG row (with its Deal ID in col D) exists.
- **Secrets in ScriptProperties, never in the repo or a sheet.** `PD_API_TOKEN`, `PD_COMPANY_DOMAIN`, `PD_DEFAULT_PIPELINE_ID` / `_STAGE_ID` / `_CURRENCY` (read by `pdGetSecrets_`). `setupPipedriveSecrets(token, domain, pipelineId, stageId, currency)` validates the token+domain with a live `GET /users/me` **before** saving (nothing persisted on failure); `getPipedriveStatus()` returns connection state for the UI and **never echoes the token**.
- **`pdFetch_` never throws** — returns `{ok, status, data, error}` (token via `?api_token=`; v1 base by default, **v2** for org custom-field reads, product variations, and the org-scoped catalog; one bounded 429 backoff). Treats a response as ok unless it's an HTTP error or carries an explicit `success:false`, so it handles **both** the v1 envelope (`success:true`) **and** v2 (which omits `success` and signals errors via HTTP status). Same isolation discipline as the non-fatal stats/dashboard writes — an API failure can't fail a run.
- **Global deal-field rules — a new `PIPEDRIVE_SETTINGS` key/value tab** in SF_DEALER_CONFIG (auto-created by `getOrCreatePipedriveSettingsSheet_`). Row `deal_field_rules` is a JSON array of rules, each setting **one** deal field in one of **three modes**:
  - **copy** — `{id, deal_field, type, mode:"copy", org_field, option_map?}`: read the org's custom field and copy it (`option_map` translates an enum/set option id; `type:"monetary"` writes the `<deal_field>_currency` companion).
  - **conditional** — `{id, …, mode:"conditional", group, then_value, else_value}`: IF the org's fields match `group` THEN/ELSE. `group` is the targeting-rule shape (`{match:"all"|"any", children:[{field,op,values}|nested]}`) but each `field` is a **Pipedrive org-field key**.
  - **constant** — `{id, …, mode:"constant", value, if_empty}`: a fixed value, **no org needed**. `if_empty` makes one rule express create-vs-link — on a **New Deal** the value is **always set**; on a **Link** it's set **only if the deal field is empty**, and **skipped, never overwritten, if the current value can't be read** (fail-safe). *Motivating use: `Proof` = `"No Proof Required"` on every new deal, only-if-empty on a link — one global rule, nothing `Proof`/dealer-specific in code.*
  - Server: `getPipedriveGlobalRules_` / `getPipedriveGlobalSettings` / `savePipedriveGlobalSettings` (assigns stable `id`s — `r1`, `r2`, … — so per-dealer overrides keep referencing the right rule), `getPipedriveSettingsBootstrap(refresh)`. Generic key/value accessors `getPipedriveSettingValue_` / `setPipedriveSettingValue_` read/upsert any row (`product_org_field`, `install_cost_config` use them too).
- **Org-condition engine — a PARALLEL mirror of the targeting engine, on org fields.** `pdOrgConditionMatches_` (leaf) + `pdOrgGroupMatches_` (recursive AND/OR) read an org's `custom_fields` by key and **fail SAFE** (unknown field/op, empty values, unparseable number, empty group → no match). Helpers `pdOrgFieldValue_`, `pdSetDealField_` (monetary `_currency` companion), `pdCollectOrgKeys_`. **The production targeting engine (`conditionMatches_` / `groupMatches_` / `ruleMatches_`) is byte-for-byte unchanged** — a deliberate parallel implementation, not a refactor.
- **`PIPEDRIVE` config tab — one row per `(dealer_key, group)`, cols A–L (12)** (auto-created by `getOrCreatePipedriveSheet_`): `dealer_key, group, org_id, org_name, product_map` (JSON; now `{type:{product_id, variation_id?, schema?, utm?}}` — see "product map is now the sole per-type config")`, deal_title_template, pipeline_id, stage_id, currency, field_overrides` (col J, JSON keyed by global rule `id` → `{off:true}` or a full replacement rule; **was `field_map` in v1**)`, active, source_product_map` (col L, `PDCFG.SOURCE_PRODUCT_MAP = 11`). A PRIMARY row plus one row per `billing_split` group — this is how Mercedes-Benz of Creve Coeur's **two Pipedrive orgs** (MB Creve Coeur + Sprinter Creve Coeur) are handled, each group row carrying its own org **and** product map. Server: `getPipedriveDealerConfig_`, `getPipedriveDealerEditorData` (also returns `globalRules` + `sourceSplit`), `savePipedriveDealerConfig`.
- **Org→deal-field resolution — `pdResolveDealFields_(orgId, globalRules, overrides, currency, isNewDeal, existingDealFields)`** (replaced `pdResolveFieldMap_`): build the effective rule list (override `{off:true}` drops a rule, a replacement rule swaps it, else keep) → apply **constant** rules first **without** the org (`if_empty` via `isNewDeal`/`existingDealFields`) → only if copy/conditional rules exist, read the referenced org keys and evaluate. Returns a **flat top-level** `{dealFieldKey: value}` map (+ `<key>_currency`) — v1 deals take custom fields as top-level 40-char hash keys, **not** nested under `custom_fields` (that's v2-only). Helpers `pdFieldEmpty_`, `pdHasIfEmptyConstant_`, `pdOptionId_`. **No global rules / all overridden off = a no-op**, so the push works before any field mapping exists.
- **Product variations (v2, lazy).** `product_map` / `source_product_map` values can pin `{product_id, variation_id?}` (bare id tolerated). Variations are a **separate v2-only endpoint** — `getProductVariations(productId)` / `pdListProductVariations_` lazily `GET /api/v2/products/{id}/variations`, fetched only for products that actually pin one. A variation carries its own `prices[]`; line-item idempotency keys by **product + variation**.
- **`pushRunToPipedrive(dealerKey, runRowIndex, mode, existingDealId)`**, `mode` ∈ `create` | `link`. Resolves the group from the RUN_LOG note (`SPLIT:<group>` → that group, else PRIMARY) and reads the matching BILLING / BILLING_<group> sheet. `create` makes a new deal on the group's org; `link` attaches + sets fields on a supplied existing deal (doesn't change its org). Composed of reusable helpers (no behavior change): `pdResolveRunContext_` (config/org gate + currency + line items, read-only, incl. `source_split`), `pdResolveDealId_` (create-or-validate, persists nothing — the caller owns the col-D write), `pdCheckInactiveProducts_`, and `pdApplyDealContents_` (attach products + set fields + install cost + Design variation + billing PDF, idempotent via the passed `state`).
- **Method-first finalize — each post-run card leads with a New Deal / Existing / Test choice + one Finalize button.** Method availability from `getRunPushModes(dealerKey, group)` → `{test, newDeal, existing, reason}` (Test always; New/Existing require a connected Pipedrive **and** an active org config), attached as `pushModes` on each `pendingRuns` entry by `runDealer`.
  - **New Deal — `finalizeRunNewDeal(dealerKey, entry)`** creates the deal **first** (a real numeric id — nothing typed), finalizes the run with it (so the "no RUN_LOG row without a real deal id" invariant holds with no placeholder), then attaches products + sets fields. The deactivated-product preempt (`pdCheckInactiveProducts_` → `stage:'inactive_product'`) runs **before any deal is created**. Retry-safe across the create-before-row reorder via a **`pd_new_<outputDocId|group>` token cache** (`pdNewDealCacheGet_` / `…Set_` / `…Merge_` / `…Clear_`): the deal id (then `rowIndex`) is cached the instant Pipedrive returns it, so a retry **adopts** it instead of creating a 2nd deal/row; cleared on success with the row-keyed `pd_push_<row>` state.
  - **Existing — `finalizeRunExisting(dealerKey, entry, existingDealId)`** validates the id via `pdGetDeal_` **first** (no row written for a bad id), finalizes with it, then links products via `pushRunToPipedrive(…, 'link', id)`.
  - **Test — `finalizeRun(dealerKey, entry, 'test')`** never touches Pipedrive, and a `test` run is **excluded from VIN-log commit** (`commitLatestRun` throws on a `test` col-D; `commitRunRows` skips it, returning `skippedTest`; the VIN Logs UI disables Commit).
- **Idempotent — failure never loses a run.** Deal ID written to **RUN_LOG col D the instant Pipedrive returns it**; a **numeric col D = "deal already created"** (dup guard — a retry never makes a second deal). A `pd_push_<row>` ScriptProperties record tracks `productsDone` / `fieldsDone` / `installDone` / `designDone` so a retry **resumes** mid-push; `pdAttachProducts_` skips a product+variation already on the deal. On failure → error + `retryable` flag, **no rollback**.
- **Org-scoped product picker + a global `product_org_field` setting (with auto-detect).** Pipedrive products are a **global catalog with no native product↔org link**, so the per-dealer picker scopes via a product **Organization-type custom field** (the "Customer" field). `getEffectiveProductOrgField_()` = the explicit `product_org_field` setting if set, **else AUTO-DETECT** the first product custom field with `field_type` `org`/`organization` (one Customer field ⇒ zero config). When in effect, `pdListProducts_` fetches via **v2 `/products?custom_fields=<key>`** and enriches each product with `customerOrgId` (`pdExtractOrgId_`). `pdProductVisible_(prod, groupOrgId, showAll, savedProductId)` scopes by `customerOrgId === groupOrgId`, with a per-group **"Show all products"** escape hatch and **always keeps an already-saved `product_id`** (renders a filtered-out one as "Product #N (saved)" — never drops a mapping on save). `savePipedriveProductOrgField` busts the `pd_catalog_v2` cache. Config UI live from PD: `getPipedriveConfigBootstrap(refresh)` (cached ~10 min) + `searchPipedriveOrganizations(term)`.
- **Bulk dealer → organization linker (one-time, review-gated setup helper).** `getDealerOrgLinkProposals()` is **READ-ONLY** — proposes an org per active dealer by name (`pdListAllOrganizations_` + `normalizeOrgName_` / `matchOrg_`; `matchType` exact/strong/weak/none), writes nothing. `saveDealerOrgLinks(links)` upserts **only each dealer's PRIMARY-row org** (preserves `product_map` / `field_overrides`; creates a PRIMARY row if absent; never touches product maps or split-group rows). UI: a "Link dealers to organizations" card in `ViewPipedriveSettings.html` (Scan & match → review table → Save confirmed links).
- **Install-cost line item + No-Charge Design variation on every push** — config-driven off the `install_cost_config` row (`PD_INSTALL_COST_KEY`; `getInstallCostConfig_` / `getPipedriveInstallCostConfig` / `saveInstallCostConfig`), nothing dealer-/id-hardcoded; inert until set. Both run inside `pdApplyDealContents_` after products + fields, each gated by its own `state` flag (`installDone` / `designDone`).
  - **`pdApplyInstallCost_(dealId, pdCfg)`** reads the org's "Program Install Cost" enum, looks up the option, and **adds-or-updates** the Install product line (idempotent). Price = `percent × subtotal` (subtotal **excludes** the design + install products), rounded to the cent, else 0.
  - **`pdApplyDesignVariation_(dealId)`** — a PD automation adds the "Design" line a few seconds post-create, so this **polls** (~8×2s) and sets the No-Charge variation **only if the Design line's variation is empty** (`pdFieldEmpty_`); `designPending` if the automation hasn't fired (a re-push sets it).
  - New line-item API helpers: `pdUpdateDealProduct_(dealId, attachmentId, body)` → `PUT /deals/{id}/products/{attachmentId}` (**first line-item UPDATE in the codebase — keyed on the deal-product ATTACHMENT id, distinct from `product_id`**); `pdAddDealProduct_` → POST one line (no dedup).
- **Auto-generate a formatted PDF of the run's BILLING sheet and attach it to the deal on every push** (replacing the manual download-the-CSV step). Best-effort / non-fatal: a `state.billingPdfDone` step in `pdApplyDealContents_`, gated by the `runCtx` param; a failure flags `billingPdfPending` (the push still succeeds; a re-push retries). Idempotent — `pdDealHasBillingPdf_` checks for a **date-free** filename (`Billing - <dealerName>.pdf` / `Billing - <dealerName> (<GROUP>).pdf`) so it matches across days. `readBillingForPdf_` parses the rendered sheet (checks `DUPLICATES` before `BY TYPE` — substring trap), `buildBillingPdfTab_` lays it out in a temp tab (the produced-VINs grid via `billingVinGrid_`, ≥15 per column before wrapping, capped at 6 columns), `exportSheetPdf_` exports via the Sheets PDF URL, `pdAttachFileToDeal_` uploads via `POST /api/v1/files` multipart (the **first file upload in the codebase**). Orchestrated by `attachBillingPdfToDeal_`. **Verified live.**
- **UI + nav.** New global **`ViewPipedriveSettings.html`** (connection setup, the `product_org_field` picker, the bulk dealer→org linker, the Install Cost card, and the global deal-field rules builder with copy / conditional / **Set value** modes — the constant value picker reuses `psRenderValuePicker_` + an "only set if empty" checkbox). The **Dealer Rules → Pipedrive panel** (`ViewRules.html`, per-dealer) has read-only connection state, a per-group org picker, an org-scoped type→product grid with a variation selector, the secondary product grid for a `source_split` dealer, and per-field **overrides** (Use-global / Off / Override…) whose inline editor **reuses a shared, context-parameterized rule-card editor** (`psRenderRuleCard_` / `psSerializeOneRule_` / `psDeserializeOneRule_` / `psRegisterRuleCtx_`) shared with the global screen. `App.html` gained a collapsible **System Settings** sidebar group (`toggleSettingsGroup`) housing Dealer Rules, Normalization, Data Sources, and Pipedrive Settings. ViewRun finalize cards drive the push; ViewVinLog has a per-run push/retry action.
- **The Pipedrive product map is now the SOLE per-type config — `type_rules` (DEALERS col O) is no longer read by the run.** Each `product_map[type]` (and `source_product_map[group][type]`) entry now holds `{product_id, variation_id?, schema?, utm?}`, carrying both the CSV **schema** (layout/grouping) **and** the QR **UTM** — so the one mapping the user already picks for billing is the single source for billing, CSV layout, grouping, and UTM. (The product is the real-world output unit, i.e. a template; the CSV schema is not a template, so the product carries the template identity.)
  - `runDealer` dropped the top-of-function `getTypeRules_(config)`; after the ORDERMATCH match it fetches the product maps (`getCsvProductMaps_`), **validates** via `validateProductMapForRun_(matchedTypes, mainProductMap)`, then **builds the run's type rules from the product map** via `buildTypeRulesFromProductMap_` (one synthetic rule per mapped type, **ordered CPO-EL before CPO** because `matchRule_` is substring-based) and proceeds. `buildLinks_` / `buildUtmFormula_` (QR UTM) and `buildCSVSheet_` / `csvOutputGroups_` / `resolveRuleSchema_` (CSV schema/grouping) work **unchanged** — only the *source* of the type rules changed.
  - If any matched vehicle's type has **no product or no schema**, the run **throws** a clear Error ("Cannot run `<dealer>` — no product/schema set for type(s): X. Set them in Dealer Rules → Pipedrive.") — caught by the existing run try/catch and shown in the run modal. So the run now **requires a complete product map for every dealer (even test runs)**.
  - `migrateTypeRulesIntoProductMap()` (run once from the Apps Script editor) backfills each product-map / source-product-map type's `schema` + `utm` from the legacy `type_rules` (col O) via `matchRule_`, **only where a product is already mapped**, never overwriting; idempotent. `getTypeRules_` is kept **only** for that migration.
  - UI (`ViewRules.html`): a per-product **UTM** text column added to both product grids (round-tripped via `normalizeProductMap_` / `buildProductMap_` like `schema`); the **entire Type Rules editor panel is deleted** (`availableSchemas` kept for the product picker's Schema column).

### Changed
- **Pipedrive line-item quantity is GROSS, not net of dupes.** `buildLineItems_(billing, productMap, products, currency, variationsByProduct)` no longer subtracts the per-type VIN-log dupe counts — the quantity sent for each product is **every matched/produced car, including cars flagged as duplicates because their VIN is already in the VIN log** (a re-printed VIN is still produced and billed). Reads the gross `totalNew` / `totalPO` / `totalCpo` / `totalCpoEl` from `readBillingTotals_`. Applies to **all dealers**. Same product+variation summed; different variations stay distinct; `item_price` from the variation's (then product's) `prices[]` by currency.
- **Two product-partition axes, both driven generically off `filtering_rules` (no per-dealer code):** `billing_split` (separate deals, each its own org + product map — e.g. MBCC) and `source_split` (one deal, separate products per source via `source_product_map` col L — e.g. Frank Leta's secondary CSV output on the same deal). The Dealer Rules → Pipedrive screen renders the right product config for either automatically. Source-split push merges `product_map × main qty` + `source_product_map[group] × secondary qty` by product+variation (`readBillingBySource_` → `bySourceToBilling_` → `mergeLineItems_`); `renderBillingSheet_` now also writes the BY SOURCE section on a `billing_split` group sheet so a future dealer with both splits works.
- **Run-Order finalize is method-first** — replaces the v2.10 "type a deal ID → Finalize → separate Push to Pipedrive block" flow (a brand-new order previously had no id to finalize with except the `test` placeholder). The push is now invoked **from** the finalize action (see `finalizeRunNewDeal` / `finalizeRunExisting` / `getRunPushModes` under Added). `pushRunToPipedrive`'s signature, gates, and return shapes are unchanged, so the ViewVinLog push + link/retry paths are untouched.

### Fixed
- **Pipedrive requires INTEGER ids — coerce the string-stored ids on send.** Product/variation ids and enum/set option ids live as strings in the config; Pipedrive rejects them (`body/product_id must be integer`), so the first live push **created the deal but attached no products and set no fields**. `pdAttachProducts_` now `Number()`s `product_id` / `product_variation_id` / `item_price` / `quantity`; `pdSetDealField_` + `pdResolveDealFields_` coerce enum/set option ids + monetary amounts via `pdOptionId_` (`text`/`varchar` left as strings, so a numeric-looking text value isn't mangled). A retry attaches to the already-created deal (the numeric col-D dup guard adopts it — no second deal). *(org_id was already `Number()`-coerced at deal creation, which is why the deal itself created fine — the asymmetry was the tell.)*

### Removed
- **`type_rules.csv_schema` as a run-time CSV-schema fallback** — the product map's `schema` is now mandatory per matched type (a missing one blocks the run); col O `type_rules` is dormant.
- **The `*` catch-all from the run's type matching** — an unmapped or normalization-missed type now blocks the run with a prompt instead of falling through to a catch-all schema.
- **The Type Rules editor panel** in `ViewRules.html` — per-type schema + UTM are now columns on the product picker in the Pipedrive panel.

---

## [2026-06-12] — v2.10: Post-run finalization (deferred deal IDs + abandonable runs)

### Changed
- **RUN_LOG and ORDER_STATS are now written only at a post-run finalization step — never during the run.** `runDealer` step 17 builds self-contained `pendingRuns` entries (one per prospective log row; two for a billing-split run with group units) and returns them to the modal instead of writing rows. New `finalizeRun(dealerKey, entry, dealId)` writes one RUN_LOG (+ ORDER_STATS) row per finalized entry via the unchanged `writeRunLog_`.
  - **Invariant: no RUN_LOG row is ever written with a blank deal ID.** Finalizing requires one; enter `test` for test runs.
  - **Invariant: the VIN log is never written implicitly.** Finalizing only logs the run; the VIN log is touched solely by the explicit "✓ Add to VIN Log" button or the VIN Log Updater.
  - Motivating case: Pipedrive deals are only created once an order is known to exist — a zero-match CAO run (or the Sprinter half of an MBCC run with no vans) no longer demands a deal ID that would have to be cancelled.
- **Deal ID fields are now optional at run time for all dealers** (primary and split). They remain on the pre-run form purely to pre-fill the finalization cards; the Run button is gated on user + dealer + VINs only. Server-side required-throws removed from `pasteVinsAndRun`.
- **Post-run panel reworked into finalization cards** (DealerSelector.html): one card per prospective entry showing label + unit counts, a deal ID input (pre-filled when entered up-front), and **Finalize** / **Abandon** buttons. Cards act independently — e.g. abandon a 0-match primary and finalize the Sprinter half. "✓ Add to VIN Log" enables only once ≥1 card is finalized (0-VIN finalized rows are excluded from commit). Discard guards (confirm dialogs) protect un-finalized results on dealer change, on starting a new run, and on the Cancel/Close button. The dialog's X-close cannot be intercepted (GAS limitation) — an X-closed run is simply never logged; recovery is re-running or the VIN Log Updater's manual-entry panel.

### Added
- **`abandonRun(dealerKey, outputDocId)`** — when abandonment leaves no live cards (nothing finalized, nothing pending), the run's artifacts are deleted after a warning popup: the output doc and the dealer's `<prefix>_QR_Code_N.PNG` files are moved to **Drive trash** (recoverable 30 days, not a hard delete). A partially-abandoned split run (sibling card finalized or pending) keeps the shared doc/QRs and the abandon is bookkeeping-only — a distinct popup explains the difference.

### Fixed
- Typing in the pre-run deal ID fields after a run no longer clears the post-run state (the old input listeners called `hidePostRunActions()` on every keystroke, which would have silently discarded pending finalization cards).

### Behavior changes (documented)
- Running `runDealer` directly from the Apps Script editor no longer writes RUN_LOG — it returns `pendingRuns` without logging.
- An abandoned run leaves no trace in RUN_LOG/ORDER_STATS; a run discarded by closing the modal leaves its output doc in Drive (aged out by `cleanUpOutputDocs`).

---

## [2026-06-12] — v2.9: Billing split for shared-feed dual accounts (MBCC/Sprinter)

### Added
- **Billing split for shared-feed dual accounts (MBCC / Sprinter of Creve Coeur).** One run, one CSV (one Illustrator setup) — but billing separated per account. New optional `billing_split` key in `filtering_rules` (col W), no per-dealer code:
  ```json
  "billing_split": { "group_name": "SPRINTER", "deal_label": "Sprinter Deal ID",
                     "field": "model", "op": "contains", "values": ["Sprinter", "Metris"] }
  ```
  - `getBillingSplit_(config)` parses/validates (fields `model/make/trim/type` — ORDERMATCH vehicle keys, intentionally not `FILTER_FIELD_INDEX`; ops `contains`/`in`, case-insensitive, any-value OR). **Fail-safe:** absent or malformed → run behaves exactly as today.
  - `writeBillingSheet_(outputDoc, billingSplit)` partitions matched vehicles via `isInBillingGroup_`; the five-section layout is extracted into `renderBillingSheet_(sheet, …)` and rendered twice: **BILLING** (primary account, excludes group units; also carries the not-found list, which can't be classified) and **BILLING_<group>** (created on demand with `insertSheet` — the universal template is intentionally untouched). Sums across both sheets equal the old single-sheet totals. `readBillingTotals_` gains an optional `sheetName` param.
  - **Two Pipedrive deal IDs per run:** Run Dealer modal shows a second required deal ID field (label from `deal_label`) only for split dealers (`getActiveDealersForUI` now returns `splitDealLabel`); `pasteVinsAndRun`/`runDealer` gain a trailing `splitDealId` param with server-side validation.
  - **Two RUN_LOG rows per split run** (same `output_doc_id`/dealer_key; notes col U = `SPLIT:PRIMARY` / `SPLIT:<group>`): each row carries its own deal ID, per-account totals, and produced VINs, so VIN Log Updater commit/rollback works per account **unchanged** (rollback keys on deal ID + committed_at). Zero group units → second row skipped (note `split: 0 <group> units` instead). ORDER_STATS likewise gets one clean row per account. Caveat: sheet-side QUERYs counting RUN_LOG rows see a split run as two rows — filter on `notes` if needed.
  - VIN log: single dealer tab as before — group VINs commit under the group's deal ID; dedup/duplicate-flagging is identifier-based and unaffected.
  - Post-run "✓ Add to VIN Log" commits both rows via new `commitRunRows(dealerKey, rowIndexes)` (skips already-committed rows, so retry after a partial failure is safe). VIN Log Updater shows a `SPLIT:*` badge per row (`getRunsForDealer` now returns `note`).
  - Split applies even with "Bypass filtering rules" — it's billing-time classification, not a filter.

### Fixed
- **Rules Editor silently dropped unknown `filtering_rules` keys on save.** `collectFilteringRules()` rebuilt the JSON from UI-managed keys only, so any passthrough key (now: `billing_split`) was erased by the next Filtering save. The editor now stashes unmanaged keys on load and re-merges them on save.

### Config
- MBCC (`MERCEDES_CREVE_COEUR`, DEALERS row 25): `billing_split` added to `filtering_rules` (`field: model, op: contains, values: ["Sprinter", "Metris"]`); notes column updated. Resolves the long-standing "MBCC/Sprinter shared inventory" item — Option B (one run, two billing outputs).

> The "second deal ID **required** at run time" behavior described above was superseded the same day by v2.10 (deal IDs optional pre-run; entered at finalization).

---

## [2026-06-11] — v2.8: Multi-file import + modal layout rework

### Added
- **Multi-file import with Replace/Merge modes** (`ScraperImport.html` + `importScraperData` rewrite):
  - **Main Import (Replace)** — select 1+ CSVs, merged into one dataset, clears SCRAPERDATA (previous behavior, now multi-file). **Merge with Existing** — combines the selected file(s) with the current SCRAPERDATA contents.
  - **VIN dedup/conflict engine** (`dedupeScraperRows_`, server-side, post-normalization): same VIN + identical data kept once silently; differing data → 2-way conflict (incumbent vs newest challenger) resolved per-VIN in a new conflict panel (side-by-side diff of only the differing fields, sources labeled by filename, bulk Keep-All buttons via the `resolutions['*']` fallback). Tolerant comparator `cellsEqual_` (trim-string or both-numeric) prevents false conflicts from `getValues()` numeric coercion.
  - **Two-phase protocol**: phase 1 detects conflicts and returns them with **zero mutation**; phase 2 re-sends the payload + resolutions, verified against an optimistic-concurrency token under a `LockService` script lock. All mutations moved below the gate — fixes the latent hazard where the old importer cleared SCRAPERDATA *before* processing (a mid-pipeline error left the sheet empty).
  - Per-file header mapping (files may differ in column order/set), per-file preview cards, all-or-nothing validity gate (a file without a VIN column blocks the import), **UTF-8 BOM strip** per file (fixes a latent silent VIN-unmatch bug), and a **mid-file header guard** (drops stray "VIN" header rows from concatenated exports).
  - Final dataset **grouped by Location** before writing (preserves `getDealerScraperData_`'s contiguity invariant in both modes); stats/health/dashboard computed on the final dataset so IMPORT_STATS baselines stay sane in merge mode. Review panel gains mode-aware totals + Import Summary badges (files, mode, duplicates removed, conflicts resolved, rows without VIN).

### Changed
- **All five modals resized to a uniform 1400×900** (`MODAL_WIDTH`/`MODAL_HEIGHT` constants): Run Dealer, Import Scraper Data, Normalization Maps, VIN Log Updater, Dealer Rules Editor. The browser viewport is the effective cap; verify on the smallest screen used to run orders.

### Fixed
- **"View Run Log" menu item did nothing.** `openRunLog()` called `.activate()` on a sheet obtained via `openById()` — activation only moves the UI on the active-spreadsheet instance. Switched to `getActiveSpreadsheet()`; the menu item now jumps to the RUN_LOG tab as intended.

---

## [2026-06-10] — v2.7: Targeting rules + Dean Team Brentwood + NORM_MAPS performance fix

### Added
- **`refreshNormReference()` + "Refresh Norm/Field Reference" menu item** — on-demand, static replacement for the NORM_MAPS cols E+ reference. Scans SCRAPERDATA once and writes sorted distinct values per column (Type/Make/Model/Trim/Status/Body Style/Fuel Type) with counts + a timestamp; zero ongoing recalc cost. Doubles as the lookup for exact raw values when authoring targeting conditions.
- **Generalized targeting rules in `filtering_rules`** — two new optional keys, fully configurable in the Edit Dealer Rules modal (Filtering tab), no per-dealer code:
  - **`conditions`** — array of generic field criteria `{field, op, values, applies_to?}`. `field` ∈ type/year/make/model/trim/ext_color/status/price/body_style/fuel_type/msrp (mapped by new `FILTER_FIELD_INDEX` constant); `op` ∈ in/not_in/contains/not_contains (string, case-insensitive) or gte/lte (numeric, price-safe — strips `$`/`,`); `applies_to` scopes a condition to specific types. New helper `evaluateCondition_` is **fail-open** on misconfiguration (unknown field/op, empty values, NaN) so a typo can never empty a dealer. Applied in **both** CAO and run phases (Bypass checkbox overrides).
  - **`cao_exclude_types`** — types skipped during CAO auto-fill only; still print when entered manually ("manual-only type"). Applied in the CAO phase only.
  - `applyFilteringRules_` gains a `phase` param ('cao'/'run'); `getDealerFilterRules_` parses both keys (default `[]`); `getCaoVins` tallies rejection reasons dynamically; `getRulesEditorBootstrap` returns field/op metadata so the UI dropdowns derive from the engine's single source of truth. `RulesEditor.html` adds a Targeting Conditions table + "Exclude from CAO Auto-Fill" pills; `DealerSelector.html` CAO summary renders the new `cond:*` / `cao_excluded` reasons. Fully backward compatible — dealers without the keys are unchanged.
- **New dealer: Dean Team Brentwood** (`DEAN_TEAM_BRENTWOOD`) — 43rd dealer row in DEALERS. ORDERS col AQ (the ORDERS sheet was widened from 42 to 43 columns), used-only (`allowed_types: ["PO","CPO"]`, `require_stock`/`require_price` true), Pipedrive deal IDs, `scraper_location_name` = "Dean Team Brentwood", CAO order entry, VIN log tab created. Drive output + QR folders created under the global OUTPUT folder.
- **New field code `PRICE_TAGLINE`** — mapped at ORDERMATCH col 21 (U). ARRAYFORMULA renders a price-tier tagline from `PRICE_RAW` (col H): `≥ $15,000` → "as low as $300/mo"; `$10,000–$14,999` → "Below $15,000"; `< $10,000` → "Below $10,000"; non-numeric → blank. Added to `FIELD_TO_COL`.

### Changed
- **Removed the live `UNIQUE()` reference formulas from NORM_MAPS cols E+** (SF_DEALER_CONFIG). At 10k+ SCRAPERDATA rows, recalculating these volatile full-column formulas made programmatic access to SF_DEALER_CONFIG time out (~100s `Service Spreadsheets failed`), breaking every config-reading modal (Run Dealer, Rules Editor, Norm Manager) and `importScraperData`, while the browser UI stayed fine. Replaced with the on-demand `refreshNormReference()` writer (see Added). Docs/invariants updated accordingly.

### Fixed
- **`PRICE_TAGLINE` returned blank for every row.** `PRICE_RAW` (ORDERMATCH col H) is stored as text, so the original `ISNUMBER(H2:H)` guard was always FALSE. Reworked the formula to coerce with `VALUE()` inside `IFERROR` (`IFERROR(IF(VALUE(H2:H)>=15000,…),"")`). A plain text-vs-number comparison was avoided because Sheets sorts any text above any number, which would bucket every text price into the top tier. Fixed in the SF_UNIVERSAL_TEMPLATE ORDERMATCH U2 cell (no code change).
- **New CSV schema `SCP_TAGLINE`** — the SCP layout plus `PRICE_TAGLINE` appended as col_11. Used by Dean Team Brentwood; leaves the shared `SCP` schema untouched.

---

## [2026-06-09] — V2 pre-release backfill

Consolidated record of recent V2 development completed prior to changelog adoption. V2 is in final bug-hunt testing ahead of production launch.

### Added
- **Multi-user QR path system**: `USER_PROFILES` tab in SF_DEALER_CONFIG, "Running as:" dropdown in the Run Dealer modal, per-user QR path resolution via `PropertiesService`
- **Produced VINs list** in BILLING sheet column B below the order summary (`── PRODUCED VINS (N) ──` header format)
- **VIN Log status strip** in the Run Dealer modal (`DealerSelector.html`): shows latest order ID and `committed_at` timestamp with a "📋 Update VIN Log" button
- **Dealer Rules Editor modal** (`RulesEditor.html`): GUI editing of per-dealer `type_rules` and `filtering_rules` without touching raw JSON
- **Duplicate detail table** in BILLING sheet at column F row 2

### Changed
- **RUN_LOG expanded from 19 to 23 columns (A–W)**: lumped totals replaced with per-type columns — `total_po`, `total_cpo`, `total_cpo_el`, `po_dupes`, `cpo_dupes`, `cpo_el_dupes`
- **CSV output consolidated by schema**: `buildCSVSheet_` groups dealers by `csv_schema`, producing one sheet per unique schema (`CSV` for a single schema, `CSV_[SCHEMA_KEY]` when multiple)
- **BILLING sheet**: all vehicle types (New, PO, CPO, CPO-EL) now always present as fixed rows, including at zero count; duplicate detection checks both VIN and Stock

### Fixed
- BILLING sheet type column references corrected

### Removed
- `QR_LOCAL_BASE_PATH` global constant (superseded by per-user path resolution)

---

*Maintenance convention: every feature/fix commit should update the `[Unreleased]` section in the same commit. On production release, `[Unreleased]` entries roll into a new dated version section.*

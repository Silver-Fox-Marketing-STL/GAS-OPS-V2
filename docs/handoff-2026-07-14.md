# Handoff — fundamentals sweep (2026-07-14)

For a fresh Claude Code session on another machine, continuing the
`feat/fundamentals-sweep` branch. Read CLAUDE.md first (auto-loaded); this doc
covers only what the repo and vault don't already record.

## Where things stand

- **Branch `feat/fundamentals-sweep` — 7 commits, complete and verified, NOT
  merged to main, NOT promoted.** `git log main..feat/fundamentals-sweep
  --oneline` lists them; CHANGELOG `[Unreleased]` describes each. Net
  +281/−1,276 across 14 files.
- Offline harness: **60/60 green** (`node test/run-tests.js`) — grew from 41
  this branch (new `pipedrive line items` + `csv groups` suites).
- Code is **already `clasp push`ed to DEV** and awaiting Nick's dev smoke.
- Origin of the work: a dual audit (tech-lead fundamentals × ponytail
  over-engineering) with cross-critique and an approved consensus plan. Full
  record in the brain vault: `01-Projects/gas-ops-v2/2026-07-13_dual-audit.md`
  and `decision-fundamentals-sweep.md` (the vault syncs via Nick's separate
  brain workspace — pull context from there, and note this session must never
  commit the vault repo).

## ⚠ Machine-local state that did NOT travel

The original machine's working tree holds **uncommitted lot-scan dev-twin
work** (modified `docs/dev-environment.md`, `lot-scan/.clasp.json`,
`lot-scan/Code.gs`; untracked `lot-scan/.clasp.prod.json`,
`lot-scan/.claspignore`, `scripts/promote-lot-scan.ps1`). It exists only
there. On this machine: don't touch the lot-scan promote flow, don't "fix"
dev-environment.md's lot-scan section against reality, and don't be surprised
that `scripts/promote-lot-scan.ps1` is missing. That work lands from the
original machine.

Also machine-local: clasp auth (`clasp login` may be needed before any push)
and each machine's QR local base path (per-user in USER_PROFILES, not repo).

## Nick's remaining manual steps (his court, in order)

Per `docs/promote-checklist.md` stages 4–10:

1. **Dev smoke** — open the dev app (DEV master sheet menu, or the dev script's
   Deploy → Test deployments `/dev` URL), run one dealer end-to-end with 2–3
   VINs from dev SCRAPERDATA, finalize as **Test**. This exercises the new
   `presetVins` handoff.
2. **Template formulas, dev first** — DEV_SF_UNIVERSAL_TEMPLATE → ORDERMATCH:
   - `T2: =ARRAYFORMULA(IF(ISBLANK(A2:A),"",IF(H2:H="*","*",IFERROR("$"&TEXT(H2:H+2000,"#,##0"),"*"))))`
   - `P2: =ARRAYFORMULA(IF(ISBLANK(A2:A),"",IFERROR(TEXT(H2:H,"#,##0"),"*")))`
   Verify a dev run's output doc (cols P/T normal, no #VALUE!), then apply the
   same two cells to the PROD template. Template/config edits don't ride
   promote.
3. **Glendale config** — prod SF_DEALER_CONFIG → DEALERS → **I13**, replace the
   cell with (drops the inert `model_trim_split` key, keeps all 5 rules):
   ```json
   {"replacements":[{"col":"model","find":"Wrangler Unlimited","model_replace":"Wrangler","trim_prepend":"Unlimited"},{"col":"trim","remove":["5'7 BOX","6'4 BOX"]},{"col":"model","find":"Grand Cherokee L","model_replace":"Grand Cherokee","trim_prepend":"L"},{"col":"model","find":"Grand Wagoneer L","model_replace":"Grand Wagoneer","trim_prepend":"L"},{"col":"model","find":"Wagoneer L","model_replace":"Wagoneer","trim_prepend":"L"}]}
   ```
4. **(Optional, checklist stage 5)** `/code-review` on the branch diff before
   merging — offered, not yet run.
5. **Merge + push**: `git checkout main && git pull`,
   `git merge --no-ff feat/fundamentals-sweep`, `git push` (house style: no-ff
   merge commits).
6. **Promote**: `.\scripts\promote.ps1` — Nick-run only, typed `PROMOTE`. New
   this branch: it runs the harness as Gate 1.5 and refuses on red. No sheet
   migrations this branch (stage 8 skipped).
7. **Prod smoke** — `/exec` app, one read-only flow. Rollback if ever needed:
   prod Manage Deployments → previous version.
8. **After promote**: update the vault's `open-issues.md` (via
   @agent-brain-manager) — several items closed/changed by this branch; the
   dual-audit and decision notes say backlog updates wait for promote.

## Open decisions & parked work (don't re-litigate the rejected ones)

- **Nick's calls, parked**: Classic fallback layer removal (needs his App
  sign-off — biggest remaining cut); `auditConfigPlaceholders` wire-the-daily-
  trigger-or-drop (if wired, its `type_rules` check should move to product_map).
- **Deliberately held**: `eomPdfSpike` (retained by the open EOM PDF-layout
  task in vault open-issues).
- **Tier 3, opportunistic only** (do when already in that code): memoize the
  col-W triple `JSON.parse`; fail-loud ORDERMATCH header assert for
  FIELD_TO_COL; per-section Code.gs lifts (never scheduled).
- **Consensus-rejected** (reasons in the vault decision log): prod-live git
  tag, scheduled monolith split, runtime FIELD_TO_COL rebuild, new failed-run
  tracking.
- This doc is transient — delete it once the branch is merged and the other
  machine has caught up.

## Suggested skills

- `brain` / `/brain-find` — pull `01-Projects/gas-ops-v2/` context (project
  brief, open-issues, the two audit/decision notes) before substantive work.
- `tech-lead` — if continuing direction/decomposition of the remaining items.
- `ponytail` — already active by default in this repo's sessions; keep it.
- `code-review` — Nick may ask for the pre-merge review (step 4 above).
- `verify` — before committing anything nontrivial on top of this branch.
